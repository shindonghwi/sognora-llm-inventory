# sg-design-humanize — CHANGELOG

## 0.2.0 — 2026-08-15 (생태계 리서치 통합 — 지식층 재구축)

기존 스킬 3종(avoid-ai-design·impeccable 59룰·Anthropic frontend-design)과 MIT 자산 2종(interface-design design-deslop, ui-ux-pro-max design-audit)을 정밀 분석해 흡수. 코어(시그니처 린트·불변 게이트·벤치마크)는 생태계에 없는 유일 자산이라 유지, 지식층을 재구축.

- 탐지 +10룰(총 35): BD1 사이드 액센트 보더 · CO5 그라데이션 텍스트 · LA4 중첩 카드 · QF1~6 품질 바닥(가로 오버플로, 정지 시 숨은 콘텐츠, 행폭, 타이포 하한, 탭 타깃, unsized img)
- Squint 패스: 렌더 크롭을 보고 구성 슬롭 5축(초점/위계/단조/색/보더 구조) 판정 — 스크립트 사각 보완
- "슬롭이 아닌 것" 오탐 억제: 동기 있는 대담함·문서화된 이탈·범위 밖·린터 소관 면책, "확신 없으면 결정으로 취급"
- 재생성 자기검증: "비슷한 브리프에 또 나올 조합이면 기본값" — 방향 확정 전 필수
- 방향 7축(+시그니처 단일 집중), 타이포 극단 원칙(무게 100-200/800-900, 점프 3배+), 반프로필에 AI 수렴 룩 세대(크림+세리프+테라코타 등) — 금지가 아니라 "기본값 소비 금지" 프레이밍
- 마감 체크리스트(상태 4종·transition:all 금지·tabular nums·동심 radius·액세서리 하나 빼기)
- impeccable CLI 병행 탐지기 옵션(Apache-2.0 별도 프로세스, file:line 보조 신호 — 게이트 SSOT는 본 스킬)
- 보고 단계에 확정 토큰의 DESIGN.md 고정(이후 이탈 감시 기반)

## 0.1.3 — 2026-08-15

- 벤치마크 프로토콜 신설(references/benchmarks.md): 개편 모드는 같은 단위·장르의 잘 만든 실물을 detect.mjs로 실측해 갭 표(폰트 단수·디스플레이 배율·밀도·자산 다양성)를 만들고 가장 벌어진 축부터 재설계 — 룰이 아니라 실측이 목표선
- 대상 단위 일반화: 랜딩 전용 아님 — 페이지(앱 화면·대시보드)/섹션/컴포넌트별 벤치마크 소스(Mobbin·Refero·Component Gallery·1군 디자인 시스템)와 측정법 구분, `범위:`·`벤치마크:` 옵션 추가

## 0.1.2 — 2026-08-15

- 치명 버그 수정: 개편 모드의 작업 범위가 티에서만 유도되어 "티 0인 허접한 페이지"가 A등급으로 통과하던 문제
  - 개편 모드 대상 = 구조성 티 섹션 ∪ 퀄리티 바 미달 섹션(자산 없음·공백 지배·타이포 부실) — 티 0이어도 전부 진행
  - 조기 종료를 티 제거 모드 전용으로 한정, 티 0이어도 퀄리티 바 미달이면 "개편 모드 권장" 고지 의무화
  - 등급 A에 (개편 모드) 퀄리티 바 5/5 필수 조건 추가

## 0.1.1 — 2026-08-15

- 개편 모드에 "퀄리티 바" 5항 신설 — 티 0은 필요조건일 뿐, 완료 조건은 디스플레이 모먼트·스케일 6±1단·자산 플랜·색 커밋·빈 공간 금지 (plai.io/omneky 실측 교훈: 퀄리티는 티 부재가 아니라 아트 디렉션)
- AS1 신규: 동일 이미지 3회+ 재사용 탐지(이미지 최적화 프록시 URL 해석 포함)
- LA1 정밀화: 실미디어(100×100+)가 들어찬 카드 나열은 면책 — 빈 카드만 티

## 0.1.0 — 2026-08-15

- 최초 생성: AI 티 디자인 감지·재설계 스킬
- 탐지 25룰(8카테고리) — 타이포 스케일·보라 그라데이션·카드 나열·번호 뱃지·의미 없는 아이콘·밀도·모션·한글 타이포. [결정적]/[혐의→LLM 확정] 이원 체계
- 게이트 3종(gate.mjs, 오프라인): ① 티 사후조건(🔴 재탐지 0) ② 시그니처 린트 — 이산 4성분(행·열/균일성/정렬/미디어) 전부 불변이면 "조정만 했음" 실패 ③ 텍스트(연접 커버+서수 삭제 허용 목록)·링크·정보성 이미지 불변
- 모드 이분: 티 제거(기본) / 개편(구조성 🔴🟡 전부 재설계, 무티 섹션은 토큰만)
- 방향은 카탈로그 복사가 아니라 프로젝트 입력(브랜드·도메인·격식·반프로필)에서 유도 — 항상 다른 결과
- 렌더 불가 시 정적 저하 모드(--static), 자산 생성은 런타임별(codex imagen / $imagen)
- 검증: 슬롭 페이지 스모크 테스트 — 색만 바꾼 조정본 11건 실패, 구조 재설계본 통과 확인
