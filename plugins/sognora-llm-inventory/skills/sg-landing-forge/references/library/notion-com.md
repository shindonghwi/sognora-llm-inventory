# notion.com 분석

장르/단위: 생산성 SaaS / 랜딩 · 분석일 2026-08-15 · 상태: **완전** (detect·probe·capture 3종 + 풀페이지 판독)

최종 URL `https://www.notion.com/` (리다이렉트 없음) · 페이지 높이 4633px · 가로 오버플로 없음 · 1440×900 라이트 단일 테마

---

## 수치 (detect.mjs 실측, 1440×900)

- 폰트 사이즈 고유값 **10단**: 12 · 14 · 16 · 18 · 20 · 22 — (빈칸) — 42 · 54 · 72 · 96. 배율 8×
  - 22와 42 사이가 통째로 비어 있다. UI 램프(12~22)와 디스플레이 램프(42~96)를 **분리된 두 개의 사다리**로 운영한다. 스캐너는 이걸 TS1(🔴)로 잡지만 실제로는 10단 난립이 아니라 6+4 구조다
- 밀도 중앙값 **65.3%** — 라이브러리 표본 중 최고 (toss 51% · linear 30%)
  - 섹션별: 히어로 101.2% · 벤토 125.7% · 후기 65.3% · 마퀴 17.1% · 푸터 10.4%
  - 100% 초과는 중첩 카운트(카드 안 카드). 실제로 읽히는 밀도는 "상단 2/3는 꽉, 하단 1/3은 비움"의 **단조 감소 곡선**이다
- 텍스트 블록 95개 · 링크 35개 · 정보성 이미지 13 · 장식 이미지 44 · 자동재생 비디오 1 · iframe 1
- 티 프로필: 🔴3 🟡8 (혐의 5건)
- 폰트 패밀리 **2종**: NotionInter(790곳) + Lyon Text(**3곳**). 웨이트 4종: 400(518) · 500(246) · 700(23) · 600(6)

---

## 실측 토큰 (capture.mjs + 직접 계측)

### 타이포 — 크기가 커질수록 트래킹이 조여지는 램프

| 역할 | 크기/행간 | 웨이트 | letter-spacing | 색 |
|---|---|---|---|---|
| H1 히어로 | 96/100 (1.04) | 600 | **-4.6px** (-0.048em) | rgba(0,0,0,.95) |
| H2 섹션 | 54/56 (1.04) | 700 | **-1.875px** (-0.035em) | rgba(0,0,0,.95) |
| H2 최종 CTA | 42/48 (1.14) | 700 | -1.5px (-0.036em) | rgba(0,0,0,.95) |
| H2 벤토 카드 | 22/28 (1.27) | 700 | -0.25px (-0.011em) | #000 |
| 리드 본문 | 20/28 (1.40) | 400 | -0.125px | rgba(0,0,0,.898) |
| 카드 제목 | 16/24 (1.50) | 700 | normal | #000 |
| 기본 본문·내비 | 16/24 (1.50) | 400 | normal | rgba(0,0,0,.898) |
| 아이브로우·캡션 | 14/20 (1.43) | 400 | normal | rgba(0,0,0,.54) |
| 인용문 | 18/28 (1.56) | 400 | normal | **Lyon Text** / #fff |

행간은 96px에서 1.04, 16px에서 1.50 — 크기와 반비례로 벌어진다. 트래킹은 22px 아래로 내려가면 `normal`로 손을 뗀다.

### 색 — 중립 서피스가 딱 3개

- 페이지 `#FFFFFF` · 카드면 `#F9F9F8` · CTA 밴드 `#F6F5F4`. 픽셀 샘플링으로 재확인, 그 외 중립 배경 없음
- 잉크 4단: `rgba(0,0,0,.95)` 제목 / `rgba(0,0,0,.898)` 본문 / `rgba(0,0,0,.54)` 캡션 / `#A39E98` 흐림
  — 흐림 계열이 `#A39E98`(따뜻한 회색)이다. 순회색이 아니다
- 블루 3단: `#0075DE` 주 버튼 / `#097FE8` 링크 / `#005BAB` 호버·틴트 위 텍스트
- 캐릭터·태그 팔레트: `#FFB110` `#F64932` `#FFC95E` `#F77463` `#62AEF0` `#AD6DED` `#27918D` `#FF6D00` `#9849E8` `#9C7054`
- 회전 단어 알약 6색: `#BDE6E4`(Scale) `#E6F3FE`(Think) `#D0F4D8`(Ship) `#FFDEC4`(Create) `#FFE4AF`(Build) `#EADBFA`(Jam)
- 메뉴 아이콘 타일 틴트: `#FCF8F5` `#FFF5ED` `#FEF3F1` `#F8F5FC` `#F2F9FF` `#F2FAFA` `#FFF5E0`

### 라운드 — 5단 사다리

`4px`(내비 링크·메뉴 아이콘 타일, 45곳) · `8px`(버튼·히어로 프레임·메뉴 항목, 82곳) · `12px`(카드·메가메뉴 패널, 17곳) · `16px`(4곳) · `9999px`(알약, 13곳) · `100%`/`50%`(원형 칩·아바타)

### 그림자 — 알파 상한 0.04, 딱 2종 11곳

```
/* 소형: 카드·메가메뉴 패널 (9곳) */
0 0.175px 1.041px rgba(0,0,0,.01), 0 0.8px 2.925px rgba(0,0,0,.02),
0 2.025px 7.847px rgba(0,0,0,.027), 0 4px 18px rgba(0,0,0,.04)

/* 대형: 히어로 제품 프레임 (2곳) */
0 0.667px 3.502px rgba(0,0,0,.008), 0 2.933px 7.252px rgba(0,0,0,.016),
0 7.2px 14.462px rgba(0,0,0,.02),  0 13.867px 28.348px rgba(0,0,0,.024),
0 23.333px 52.123px rgba(0,0,0,.03), 0 36px 89px rgba(0,0,0,.04)
```

4스톱·6스톱 물리 램프인데 **어느 스톱도 알파가 0.04를 넘지 않는다.** 나머지 면 분리는 전부 `1px solid rgba(0,0,0,.1)` 헤어라인이 담당한다(7곳).

### 그라데이션 — 전 페이지에 3개, 전부 기능용

`linear-gradient(rgba(0,0,0,0) 35%, rgba(0,0,0,.55))` ×3 — 후기 카드 3장의 텍스트 가독 스크림. **장식 목적 그라데이션 0개.**

### 레이아웃

- 콘텐츠 컬럼 **1228.8px @ x=105.6** (좌우 여백 105.6)
- 히어로만 **1252px @ x=94** — 본문보다 23px 넓게 터뜨린다
- 히어로 그리드가 이름 붙인 트랙이다:
  ```
  [full-bleed-start] 30px [full-start] 64px [content-start] 122px 24px
  [center-start] 468px [center-mid-start] 24px [center-mid-end] 468px [center-end]
  24px 122px [content-end] 64px [full-end] 30px [full-bleed-end]
  ```
  `content`와 `center` 사이의 **122px 트랙 두 개가 장식 전용 레일**이다(실측 아이템 x=94 w=146 = 122+24). 손그림 캐릭터가 여기 산다
- 내비 그리드 `300.391px 759.219px 300.391px` gap 16 pad 14/24 — 좌우 컬럼을 같은 폭으로 고정해 가운데 메뉴를 광학 중앙에 박는다
- 벤토 그리드 2열 `602.406px` gap 24 / 후기 3열 `393.6px` gap 24 / 컴팩트 카드 5열 239.4px **gap 8**
- 수직 리듬: 섹션 래퍼 `padding: 96px 0` · CTA 밴드 `64px 0` · 푸터 `80px 125px` · 카드 내부 `24px`

### 컴포넌트

| 요소 | 실측 |
|---|---|
| 내비 | h64 · sticky · bg #fff · shadow `0 1px 0 rgba(0,0,0,0)` → 스크롤 시 발현 |
| 내비 링크 | h30 pad 5/10 r4 · hover bg `rgba(0,0,0,.05)` |
| 주 버튼(내비) | 132.4×36 pad 4/14 r8 · #0075DE / #fff 16px 500 |
| 주 버튼(히어로) | 155.7×38 pad 6/15 r8 · #0075DE → hover #005BAB |
| 보조 버튼(틴트) | 155.7×38 r8 · bg `#E6F3FE` / 텍스트 `#005BAB` |
| 보조 버튼(흰) | 155.7×38 r8 · bg #fff / 텍스트 `#097FE8` |
| 히어로 제품 프레임 | 960×601 r8 · border 1px rgba(0,0,0,.1) · 6스톱 그림자 · `overflow:hidden` |
| 회전 단어 알약 | h103 pad 8/28 r9999 · 단어 길이 따라 폭 238~319px 가변 |
| 벤토 카드 | 602×560 r12 · `bento_bentoInner` **overflow:hidden** · 헤더 h108 pad24 gap8 bg #F9F9F8 |
| 벤토 화살표 칩 | 32×32 r100% bg #000 / #fff 20px 600 |
| 컴팩트 카드 | 239.4×160 r12 pad24 gap16 · bg #fff · **border 1px rgba(0,0,0,.1)** (그림자 없음) |
| 후기 카드 | 393.6×492 r12 `overflow:hidden` · 콘텐츠 pad24 gap16 |
| 로고월 항목 | 66~86 × **24** · `mask-image: url(*.svg)` + bg `rgba(0,0,0,.898)` · hover bg `#F6F5F4` |
| 마퀴 | h24 gap20 margin 12/0 · `mask: linear-gradient(to right, transparent, #000 15%, …)` |
| 언어 선택 버튼 | 166×38 pad 8/16 **r9999** · 14px 500 |
| 메가메뉴 패널 | r12 · border 1px rgba(0,0,0,.1) · 4스톱 그림자 · bg #fff |
| 메가메뉴 항목 | 249×44 r8 pad `4px 16px 4px 8px` |
| 메가메뉴 아이콘 타일 | 32×32 **r4** · 파스텔 틴트 배경, 잉크색은 타일별로 다름 |

---

## 구조 (섹션 전부)

### 0. 글로벌 내비 — y0, h64, sticky

로고 / 중앙 메뉴 8개(Product·AI·Solutions·Resources 드롭다운 + Developers·Enterprise·Pricing·Request a demo) / 우측 `Log in` + 주 버튼. 3열 그리드로 중앙 정렬 고정. 스크롤하면 하단 1px 그림자가 애니메이션으로 붙는다(`navShadowScrolled`).

### 1. 히어로 — y64, h1433

위에서 아래로:
1. **아바타 파일 열** y144, 72×72 원 7개(`pile_1..7.webp`). 컬러 링(파랑·검정·빨강·노랑·검정·파랑·빨강) 안에 손그림 얼굴/사물. 헤드라인보다 먼저 나오는 "팀+에이전트 캐스트"
2. **H1** y248, 1252×210, 2줄 중앙. `Where teams and agents [Scale] together.` — `[Scale]` 자리가 파스텔 알약이고 6단어를 순환
3. **리드** y470, 20/28, 1줄
4. **CTA 2개** y518, 주(파랑) + 보조(연파랑 틴트)
5. **제품 프레임** y636, 960×601. Contentful CDN의 1920×1200 mp4를 autoplay·loop·muted로 재생 — 정지 이미지가 아니라 실제 Notion UI가 움직인다. 좌하단에 Pause 컨트롤
6. **장식 캐릭터 위성** 좌 x94/우 x1200 레일 위, y545~1255에 6기
7. **로고월** 2행 17개, `position: sticky; bottom: -48px`
8. **캡션** y1349 "Trusted by 98% of the Forbes Cloud 100" 14/20 rgba(0,0,0,.54)

### 2. "AI where your team works." — 래퍼 y1497 h1448 (pad 96/96)

- H2 y1593 54/56 **좌측 정렬** x106
- **벤토 3장**
  - A `Capture knowledge` / `Bring everything into one system of record.` 602×560
  - B `Find answers` / `Get answers, instantly—with citations.` 602×560
  - C(와이드) `Automate busywork` / `Keep work moving 24/7 with agents.` 1229폭
  - A·B는 화살표 칩이 헤더 우측, C는 **본문 아래**로 내려온다 — 레이아웃에 따라 칩 위치가 이동
- **아이브로우** y2661 `See what Notion can do` 14/20 rgba(0,0,0,.54)
- **컴팩트 카드 5장** y2689, 239.4×160. 40px 캐릭터 아이콘 + 16/24 700 제목, 제목 끝에 `→`가 인라인으로 붙는다

### 3. "Trusted by teams that ship." — 래퍼 y2945 h772 (pad 96/96)

H2 y3041 좌측 정렬 + 후기 카드 3장 y3129. 카드마다:
- 아트디렉션된 **듀오톤 인물 포스터**(Cursor=주황빨강 / Faire=파랑 / Ramp=황금) — CSS 필터가 아니라 소스 이미지 자체가 듀오톤
- 좌상단 화이트 컬러웨이 고객 로고(87×25) — 로고월의 검정판과 **다른 에셋**을 따로 쓴다
- 하단 35%부터 시작하는 검정 스크림
- Lyon Text 18/28 인용문 + NotionInter 14/20 이름·직함

### 4. 통계 마퀴 — y3729, h24

`G2's #1 knowledge base for 3 consecutive years` · `100M users in over 50 countries` · `Over 50% of YC companies` · `1.4M+ community members` · `62% of Fortune 100 use Notion` — 5개를 3배 복제한 트랙이 60s linear infinite로 흐른다. 좌우 15%가 마스크로 페이드.

### 5. 최종 CTA — y3765, h450, bg #F6F5F4 (pad 64/64)

`Get started today.` 42/48 700 **중앙 정렬** + 버튼 2개. 450px 밴드에 콘텐츠는 90px 남짓 — 밀도 17%. 페이지에서 유일하게 대놓고 비운 구간.

### 6. 푸터 — y4215, h418, bg #fff (pad 80/125)

브랜드 컬럼(워드마크 · 소셜 5 · 언어 알약 버튼 · Cookie settings · © 2026 Notion Labs, Inc.) + 링크 4열(Company / Download / Resources / Notion for). 열 제목 14px rgba(0,0,0,.54), 링크 16px 잉크. 마지막 열 끝에 `Explore more →`.

---

## 자산 운용

**캐릭터 라이브러리 하나를 3개 스케일로 돌린다.** `/front-static/agents/*.png` 한 세트(book · globe · files-v2 · checkmark · light_bulb · apple · mailbox · rock · sign)가
- 72px → 히어로 아바타 파일 (`pile_*.webp`)
- 52~56px → 히어로 장식 위성
- 40px → 컴팩트 카드 아이콘

으로 재등장한다. 새 그림을 그리는 게 아니라 **같은 배우를 다른 배역에 다시 세운다.**

**위성은 2겹 합성이다.** 손그림 캐릭터 PNG(52px) 위에 **실제 서드파티 앱 로고 SVG**(39~46px)가 겹친다 — `tasks/github.svg` `gmail.svg` `hubspot.svg` `slack.svg` `checkWhite.svg` `chartWhite.svg`. 캐릭터=에이전트, 로고=그 에이전트가 지금 쓰는 도구. 스크롤하면 로고가 갈아끼워진다(스크롤 0에서 GitHub였던 자리가 1100에서 HubSpot). **일러스트가 장식이 아니라 문장을 말한다.**

**제품 스크린샷은 씬으로 조립한다.** 벤토 하나에 실제 UI 크롭 2~4장을 겹친다 — 예: 554×554 베이스 + 554×136 핀 카드 + 452×56 챗 입력. `bento_bentoInner`의 `overflow:hidden` + `radius:12px`가 넘치는 부분을 카드 실루엣으로 잘라내 "화면 밖으로 이어지는 제품"의 인상을 만든다.

**고객 로고는 mask-image로 단색화한다.** SVG를 `<img>`로 넣지 않고 `mask-image: url(...)` + `background-color: rgba(0,0,0,.898)`. 브랜드 원본 파일을 편집하지 않고도 전부 같은 잉크로 통일되고, hover 배경색까지 CSS로 붙는다.

**세리프는 3곳뿐이다.** Lyon Text가 후기 인용문 3개에만, 그것도 18px로. 790곳이 NotionInter인 페이지에서 세리프 3곳 — 희소성 자체가 "여긴 사람 말"이라는 신호가 된다.

**히어로는 영상이다.** 정지 스크린샷이 아니라 1920×1200 mp4(autoplay·loop·muted) + 사용자용 Pause 버튼.

---

## 인터랙션

### 호버 표 (probe 14표본 + 직접 계측)

| 대상 | 변화 | 판정 |
|---|---|---|
| 주 버튼 | bg `#0075DE` → `#005BAB` | 유일한 색 변화 |
| 내비 링크 | bg `transparent` → `rgba(0,0,0,.05)` | |
| 로고월 항목 | bg `transparent` → `#F6F5F4` | |
| 벤토 카드 | **변화 없음** (자식 40개까지 확인) | 무반응 |
| 컴팩트 카드 | **변화 없음** | 무반응 |
| 후기 카드 | **변화 없음** | 무반응 |
| 로고 · Pause · 카드 내부 링크 | **변화 없음** | 무반응 10/14 |

**카드에 호버 상태가 아예 없다.** lift도 그림자 증가도 테두리 강조도 없다. 호버 어포던스는 버튼·링크·로고 같은 **원자 단위 컨트롤에만** 주고, 큰 면은 정적으로 둔다. probe가 "무반응 10건"으로 잡은 게 방치가 아니라 설계다.

### 리빌

`hiddenAtRest 1 / revealedAfterScroll 0` — **스크롤 진입 페이드업이 없다.** 요소는 처음부터 그 자리에 있다.

### 모션 언어

- transition duration 2종뿐: `0.2s`(81곳) · `0.15s`(10곳). easing은 `cubic-bezier(0.42,0,1,1)`(ease-in) 81곳 + `ease` 10곳
- transition property: `background-color`(82) · `color`(13) · `outline-color`(5) · `box-shadow`(3) · `border-color`(1) · `all`(1). **transform 트랜지션 0곳** — 움직이는 요소가 없다
- 팝오버: `popoverFadeIn` + `popoverSlideUp` **0.15s** `cubic-bezier(0,0,0.58,1)`
- 마퀴: `60s linear infinite`
- 캐릭터 위성: 진입 `0.6s ease-in-out` + **스크롤 구동** `1s linear` (`Agent_agentMarkScroll` / `agentTaskScroll` / `agentScroll`)
- 알림 카운터: `steps(7)` · `steps(10)` — 스크롤에 맞춰 숫자가 **이산적으로 딸깍딸깍** 올라간다

### 로고월 sticky 밴드

```css
position: sticky;
bottom: -48px;   /* 밴드 높이 96px의 정확히 절반 */
```

스크롤 0~400: 뷰포트 하단에 물려 **2행 중 1행만** 보인다. 800부터 풀려 캡션(y1349) 아래 자기 자리(y1385)에 정착하고 2행 전부 드러난다. 히어로를 읽는 내내 신뢰 신호를 접힘선에 붙여두다가, 다 읽으면 놓아주고 전문(全文)을 보여주는 교대다.

### 회전 단어

`Scale → Think → Ship → Create → Build → Jam` 6단어 순환. 단어마다 알약 배경색과 좌측 점 색이 함께 바뀌고, 알약 폭이 단어 길이를 따라 238~319px로 늘었다 줄었다 한다. 이건 임의의 파스텔이 아니라 **Notion 제품 안의 select 속성 태그 색 언어를 96px 디스플레이 스케일로 끌어올린 것**이다. 랜딩이 제품의 시각 문법을 인용한다.

### 메가메뉴

트리거 호버 → 0.15s 페이드+슬라이드업. 3열 그리드, 항목마다 32×32 r4 파스텔 타일 아이콘 + 제목 + 한 줄 설명. 하단에 1px 구분선과 유틸리티 2행(`New model picker. See what's new →` / `Download the Notion App →`). 패널 r12 + 1px 헤어라인 + 4스톱 그림자.

---

## 배울 것

1. **장식용 그리드 트랙을 이름 붙여 따로 판다.** `[content-start] 122px 24px [center-start]` — 콘텐츠 컬럼 바깥에 122px 레일을 정식 트랙으로 선언해두고 거기에만 일러스트를 놓는다. `position:absolute`로 눈대중 배치하는 것과 결과가 다르다: 반응형에서 장식이 본문을 침범할 수 없고, 레이아웃 계약서에 "여긴 장식 자리"가 명시된다.

2. **트래킹을 크기의 함수로 만든다.** 96px에서 -4.6px(-0.048em), 54px에서 -1.875px(-0.035em), 22px에서 -0.25px, 16px 아래는 normal. 큰 글자를 그냥 키우기만 하면 자간이 벌어져 성기게 보인다 — 이 램프 하나가 디스플레이 타이포의 완성도 절반이다.

3. **중립 서피스를 3개로 끝낸다.** `#FFFFFF` / `#F9F9F8` / `#F6F5F4`. 4633px 페이지 전체가 이 셋으로 굴러간다. 화이트 베이스에서 위계는 배경색 개수가 아니라 **여백·타이포 크기·자산 밀도**로 만든다.

4. **그림자 알파 상한을 0.04로 못 박고, 면 분리는 헤어라인에 맡긴다.** 4~6스톱 물리 램프를 쓰되 어느 스톱도 0.04를 안 넘는다. 카드 테두리는 그림자가 아니라 `1px solid rgba(0,0,0,.1)`. "떠 있음"을 표현할 때만 그림자를 쓰고 "구분"에는 안 쓴다.

5. **그라데이션은 가독성 스크림에만 쓴다.** 전 페이지 3개, 전부 `transparent 35% → rgba(0,0,0,.55)`. 장식 그라데이션 0개. 스캐너가 CO2(🟡)로 잡았지만 이게 정확히 그라데이션을 **써도 되는** 유일한 용법이다.

6. **세리프를 인용문 3곳에만 쓴다.** 790:3 비율. 보조 서체를 넓게 깔면 그냥 서체가 두 개인 사이트가 되고, 한 역할에만 묶으면 그 역할의 목소리가 된다.

7. **일러스트 자산 한 세트를 세 스케일로 재사용한다.** 72/52/40px. 자산 개수를 늘리는 대신 같은 캐릭터를 다른 배역에 다시 세워 페이지 전체가 한 세계로 읽히게 만든다.

8. **일러스트에 정보를 실어라.** 손그림 캐릭터 + 실제 앱 로고(GitHub·Slack·Gmail·HubSpot) 2겹 합성. 캐릭터가 귀여워서 있는 게 아니라 "에이전트가 당신 도구를 쓴다"는 문장을 그림으로 말한다. 스크롤에 따라 로고가 갈리며 문장이 계속 이어진다.

9. **`overflow:hidden` + `radius`로 제품 스크린샷 씬을 만든다.** 실제 UI 크롭 2~4장을 카드보다 크게 겹쳐 놓고 카드 실루엣으로 자른다. 화면이 카드 밖으로 이어지는 인상이 생기고, 납작한 스크린샷 한 장보다 깊이가 산다.

10. **호버는 원자 컨트롤에만 준다.** 버튼·링크·로고에는 0.2s 배경색 전환, 카드 면에는 아무것도 없음. 카드 lift는 안 해도 되는 일이었다는 실증 사례. transform 트랜지션이 페이지에 0곳이다.

11. **스크롤 리빌 없이도 성립한다.** `revealedAfterScroll 0`. 페이드업 없이도 4633px가 지루하지 않은 이유는 자산 자체가 움직이기 때문(히어로 mp4, 60s 마퀴, 스크롤 구동 캐릭터). **엔트런스 애니메이션과 살아있는 콘텐츠는 대체재**다.

12. **로고월을 `sticky; bottom: -48px`로 반쪽만 물린다.** 접힘선에 신뢰 신호를 붙여두다가 히어로를 다 읽으면 놓아주고 전문을 보여준다. 오프셋이 밴드 높이의 정확히 절반.

13. **랜딩에서 제품의 시각 문법을 인용한다.** 회전 단어 알약 6색은 Notion 제품 안 select 태그 팔레트다. 랜딩용으로 새 색을 만들지 않고 제품 색을 96px로 확대했다 — 랜딩과 제품이 같은 물건이라는 가장 싼 증명.

14. **고객 로고는 `mask-image` + `background-color`로 단색화한다.** 원본 SVG를 편집하지 않고 잉크색 통일 + hover 배경까지 CSS로 처리. 로고 17개를 관리하는 가장 실무적인 방법.

15. **폰트 램프를 UI/디스플레이 두 사다리로 쪼갠다.** 12·14·16·18·20·22 / 42·54·72·96. 22와 42 사이의 빈칸이 "여기부터는 다른 목적"이라는 선언이다. 10단이 난립처럼 보여도 두 램프로 읽으면 6+4다.

---

## 버릴 것

- **컴팩트 카드 5장 균일 나열** (LA1 🔴 + DE2 🟡). 239×160에 60자 이하 5장이 같은 골격으로 가로 배열 — 우리 규칙에 정면으로 걸린다. Notion은 각 카드에 서로 다른 손그림 캐릭터를 박아 겨우 구분을 만들지만, **그 캐릭터 라이브러리가 없으면 그냥 AI 티 나는 5칸 그리드다.** 자산 없이 이 형태만 베끼면 안 된다.
- **장식 아이콘 49개에 텍스트·aria 없음** (IC3 🟡 ×4 — 히어로 23 · 벤토 6 · 마퀴 15 · 푸터 5). 캐릭터 위성이 의미를 전달하는데 스크린리더에는 아무것도 안 간다. 이건 배울 게 아니라 고쳐야 할 것.
- **손그림 캐릭터 자체의 복제.** Notion 15년 브랜드 자산이다. 배울 건 그림체가 아니라 **"자산 1세트 · 3스케일 · 2겹 합성으로 의미 싣기"라는 운용 방식**이다.
- **스캐너 오탐 3건 — 인용 전 필터링할 것**
  - `TS1 🔴 폰트 10단`: 6+4 이중 램프. 난립 아님
  - `IC1 🔴 동일 SVG 3회 반복`: 마퀴 복제 트랙. 장식 그리드 아님
  - `CO3 🟡 본문 대비 1.00:1`: 듀오톤 포스터 위 흰 텍스트. 스크림이 대비를 만드는데 스캐너가 계산 못 함
  - `LA4 🟡 카드 안 카드`: 벤토 내부 제품 패널. 의도된 씬 조립

---

## 재현 노트 (Tier-R, 2026-08-17)

수집: `bundle.mjs` → `record.mjs` → `ir.mjs`. 최종 URL `https://www.notion.com/` (200, 리다이렉트 없음).
`bundle.mjs`가 `locale: ko-KR`로 열기 때문에 **한국어 화면으로 수집됐다.** 카피·행간·폰트 스택이 한글 기준이라는 뜻이고, 영문판과 타입 스케일이 다를 수 있다.

### 체크리스트

- **① tree 1440/390 · scrollHeight — 통과.** 4509 / 4066, 스크린샷과 일치. 그룹 B에서 가장 짧은 페이지이고, **모바일이 데스크톱보다 짧은 유일한 사례**다(4066 < 4509).
- **② 섹션 수 눈 대조 — 통과(Δ0).** 눈 5 vs `ir.sections` 5. **허위 섹션 0개.** 그룹 B에서 유일하게 정확히 맞았다.
- **③ @keyframes — 통과.** 34개.
- **④ 자산 rect 결합률 — 통과.** 60/103 = **58%**. 이미지 기준 59/79 = 75%.
- **⑤ hover/reveal 타임라인 — 통과, 그룹 최강.** hover 9건 중 **실변화 5건**(모두 backgroundColor, 각 10·15·12·3·20프레임). reveal 1건은 frames=2로 무변화.
- **⑥ 재현 불가 목록 — 작성.** 아래.

### IR 요약

> **2026-08-17 최종 — 유니온 커버리지 수정판 ir.json 기준이다.** 중간 재생성본에서 섹션 2·3의 경계가 내부 카드로 파고드는 후퇴가 있었으나(원인: 중첩 후보의 높이 이중 계산), **최상위 자식만 세고 커버리지를 유니온으로 계산하도록 계기가 수정되어 정확히 복구됐다.** 아래는 복구 후 최종값이며 손보정이 필요 없다.

- **섹션 5 / 눈 5 (Δ0) — ② 통과.** 컨테이너 **1252 · 1229px**. 배경은 전 구간 흰색이고 `지금 시작하세요.` 밴드만 미묘하게 밝은 회색으로 떨어진다. `layers` 비어 있음 — **허위 섹션 0**.
- **섹션 구성(최종)**: `div.heroGrid_heroGrid__v65U8` y=64 h=1153 "팀과 에이전트가 함께 하는 곳"(96/600, 슬롯 12, glass 1) · `div.homepage_main__sRG4_` y=1397 h=1424 **"팀 업무 공간에서 바로 쓰는 AI"(54/700)** · `div.homepage_main__sRG4_` y=2821 h=772 **"결과로 말하는 팀이 믿고 쓰는 Notion"(54/700)** · `section` y=3605 h=486 "지금 시작하세요."(42/700) · `nav.footerLegacy_footerInner__434Bs` y=4091 h=418.
  → **섹션 헤드 `54/700` 2건이 각각 제 섹션의 headline로 정확히 귀속됐다.** 벤토 밴드도 h=364(내부 카드 하나)가 아니라 **h=1424(밴드 전체)** 로 돌아왔다 — tree로 확인한 실제 밴드와 일치한다(`homepage_main` 안에 `bentos_bentoGrid` y=1581 h=948, 그 안에 602×560 카드 2장 + 1229×364 와이드 카드 1장).
- **커버리지 94%** — 100px 이상 공백 **1곳뿐**(y=1217~1397의 180px, 히어로와 벤토 밴드 사이 여백). 중간본의 65%·948px 공백은 해소됐다.
- **섹션 높이** 1153 / 1424 / 772 / 486 / 418. 모바일 9개(413 / 385 ×3 / 400 / 559 / 316 / 258 / 430) — **385 3연속**이 모바일 카드 규격이다.
- **타입 스케일 상위 3**: **`96/600`**(히어로, ×1) · `72/500`(×1) · `54/700`(섹션 헤드, ×2). 이어 `42/700`(CTA) · `22/700`(×3). 본문은 `14/500`(×46) · `14/400`(×13).
  → **히어로 96px는 그룹 B 최대치다**(Apple 56 · Stripe 56 · Vercel 64). 본문 14px 대비 **6.9배**.
- **duration 후보**: 200ms(×146) · 300ms(×11) · 150ms(×9) · 100ms(×1). **200ms 하나로 사실상 통일**돼 있다.
- **keyframes 34** — 명명 예시 `fadeIn`/`fadeOut`, `scaleIn`/`scaleOut`, `popIn`, `rotate`, `globalNavigation_navShadowScrolled`, `globalNavigation_slideDown`, `globalNavigation_thinkTogetherNavTokensHeroExit`, `globalNavigation_thinkTogetherNavBgHeroExit`, `globalNavigation_thinkTogetherLogoFillHeroExit`, `popover_popoverFadeIn`/`SlideDown`/`SlideUp`, `loadingDots_pulse`.
- **실행 중 애니메이션 — 이 번들에서 가장 값진 수확이다.** 스크롤 연동 "에이전트" 세트가 **enter 600ms + scroll 1000ms 짝**으로 일관되게 잡혔다:
  `Agent_agentMarkEnter 600ms` ×3 / `Agent_agentMarkScroll 1000ms` ×3 · `Agent_agentTaskEnter 600ms` ×3 / `Agent_agentTaskScroll 1000ms` ×3 · `Agent_agentEnter 600ms` ×3 / `Agent_agentScroll 1000ms` ×3 · `Agent_notifCountScroll 1000ms` ×1. 전부 `linear` — 이징을 스크롤 진행률에 맡긴다는 신호다.
- **리스너 상위 3**: **click 358** · visibilitychange 14 · pagehide 11. click 358은 그룹 B 최다(Vercel 146 · Stripe 66 · Apple 14).
- **canvas 0 · video 1**(히어로 제품 데모, 958×599 @y=537) **· img 61.**
- 폰트 6종: `NotionInter`, `Lyon Text`, `iA Writer Mono`, `Permanent Marker`, `Noto Sans Arabic`, `Noto Sans Hebrew`.

### 시각 확인 (shot-1440.png 4밴드 · shot-390.png 직접 확인)

정상 렌더. 챌린지·쿠키월 없음.

눈으로 센 밴드 **5개**: ① 히어로(+스티키 로고 마퀴) ② 팀 업무 공간에서 바로 쓰는 AI ③ 결과로 말하는 팀이 믿고 쓰는 Notion ④ 스탯 마퀴 + 지금 시작하세요. ⑤ 푸터.

처음에는 로고 월과 스탯 마퀴를 따로 세어 7로 봤는데, tree로 확인하니 **둘 다 독립 밴드가 아니었다**:
- 로고 월(`homepageLogoWall_logoWallMarqueeWrapper`, y=857 h=96)은 히어로 컨테이너(y=64 h=1333) **안에** 있고 `stickyContainer_sentinel`을 달고 있다 — **스티키로 고정되는 요소**다. 풀페이지 캡처에서 히어로 비디오 위에 겹쳐 보이는 것은 그 스티키 상태가 찍힌 것이지 레이아웃 오류가 아니다.
- 스탯 마퀴(`homepageCta_statsMarquee`, y=3605)는 CTA 섹션의 **첫 줄**이다.

즉 IR의 묶음이 옳고 내 첫 눈대중이 틀렸다. **Δ0 확정.**

**시그니처 순간(실제로 본 것만):**
- 헤드라인 안의 **알약 하이라이트** — `팀과 에이전트가 함께 [● 생각] 하는 곳`에서 `생각`만 연파랑 알약에 담기고 앞에 점 하나가 붙는다. 96px 문장 한가운데를 오브제로 만든다
- 헤드라인 위 **원형 아바타 7개** — 손그림 인물이 각기 다른 테두리 색(파랑/노랑/빨강/주황/흰/파랑/빨강)으로 겹쳐 늘어선다
- 제품 목업 주위를 도는 **떠다니는 스티커 캐릭터 4마리**(좌상 여우, 좌중 붉은 생물, 좌하 파란 폴더, 우상 보라 체크, 우하 분홍) + 손그림 화살표 — 정갈한 스크린샷의 네 귀퉁이를 일부러 흐트러뜨린다
- 히어로 제품 데모가 **정지 이미지가 아니라 비디오**이고 좌하단에 **일시정지 컨트롤**이 노출된다
- **스티키 로고 월** — 2행 마퀴(GS, kakaostyle, LG AI연구원, 당근, sendbird, DEVSISTERS, AIR PREMIA, OpenAI, Figma / CURSOR, Vercel, L'OREAL, NVIDIA, Discord, VOLVO, RIOT GAMES, TOYOTA)가 히어로에 붙어 따라온다
- **듀오톤 영상 인용 카드 3장** — Cursor는 적색, Faire는 청색, ramp는 황색으로 인물 영상을 통째로 물들이고 그 위에 인용문을 얹는다. 브랜드 로고는 좌상단
- 하단 **스탯 마퀴** — 양 끝이 페이드되며 흐르는 24px 한 줄(`Fortune 100대 기업 중 62%가 Notion 사용` 등)
- 배경 밴드 전환이 거의 없다 — 흰색 위에서 **카드의 연회색 면**으로만 구획한다

### 복합 위젯 (기계 기록 불가 → 기술로)

- **Agent 스크롤 연동 애니메이션**(위 실측 참조): `팀 업무 공간에서 바로 쓰는 AI` 벤토 안에서 에이전트 마크·작업 카드·알림 배지가 스크롤에 물려 움직인다. enter(600ms)로 등장시키고 scroll(1000ms)로 진행률에 붙이는 2단 구조이며, 3개 요소가 같은 패턴을 공유한다(×3). **이 사이트에서 가장 재현 가치가 높은 동작이고, 값이 실측으로 남은 드문 사례다.**
- **히어로 비디오 플레이어**: 자동재생 + 일시정지 컨트롤 + 포스터(`HeroMedia_posterHidden`으로 이미 숨겨진 상태 = 재생 진입 완료). 포스터→비디오 교대 로직은 미측정.
- **스티키 로고 마퀴**: `stickyContainer_sentinel` + `sentinelBottom` 두 센티넬로 고정/해제 지점을 잡는다. 마퀴 속도 미측정.
- **글로벌 내비 히어로 이탈 전환**: `thinkTogetherNavTokensHeroExit` / `thinkTogetherNavBgHeroExit` / `thinkTogetherLogoFillHeroExit` 3종이 한 세트다 — 히어로를 벗어나는 순간 내비의 토큰 색·배경·로고 채움이 **동시에** 바뀐다. `navShadowScrolled`가 그림자를 얹는다. 캠페인별 변형(`devPlatform*`)도 정의돼 있다.
- **작업 칩 5개**(제품 피드백 분류 / Slack 티켓 해결 / 보안 경고 대응 / 주간 리포트 자동화 / 나만의 개발자 도구): 클릭 시 동작 미확인.
- **영상 인용 카드 3장**: 재생 트리거·듀오톤 구현 방식(CSS filter인지 인코딩인지) 미확인.

### 재현 불가 목록

- **히어로 제품 데모 비디오** 1건 + **듀오톤 인용 영상 3건**(고객사 인물 촬영물)
- `NotionInter` · `Lyon Text` · `iA Writer Mono` · `Permanent Marker` — 전부 라이선스 폰트. 특히 히어로 96/600의 인상은 `NotionInter` 의존
- **손그림 아바타 7 + 스티커 캐릭터 4 + 화살표** — Notion 전속 일러스트레이션. 이 사이트 인상의 절반이고 **대체 불가에 가깝다**(생성으로 흉내 내면 즉시 티가 난다)
- **고객사 로고 16종**(GS · 당근 · OpenAI · Figma · NVIDIA · TOYOTA 등) 및 인용 인물·소속
- 제품 스크린샷 내부의 실제 워크스페이스 데이터
- 로그인 뒤 화면
- Agent 애니메이션의 **스크롤 매핑 구간**(어느 스크롤 범위에 1000ms를 펴 바르는지) — duration은 잡혔으나 범위는 미측정

### 판정

**리드 자격 있음 — 유지.** 2026-08-17 재평가. 체크리스트 ①~⑥ **전항 통과**이고, ②가 Δ0으로 맞는 것도 그대로다(그룹 B에서 유일). 허위 섹션 0, canvas·WebGL 0이라 재현 불가 항목이 **자산(영상·일러스트·폰트)에만 몰려 있다** — 구조는 전부 재현 가능하고 채워 넣을 것만 남는 형태다. 모션 어휘도 200ms 단일 축 + Agent 600/1000ms 짝으로 명확하다.

중간 재생성본에서 섹션 2·3이 후퇴했었으나 **유니온 커버리지 수정으로 완전히 복구됐다**(커버리지 94%, 섹션 헤드 `54/700` 2건 정상 귀속). **손보정 없이 `ir.sections` 5개를 그대로 골격으로 쓸 수 있다** — 그룹 B에서 유일하다.

**단서 둘.**
ⓐ 손그림 일러스트가 인상을 크게 지탱하므로, 자기 자산 클래스가 이를 감당 못 하면 골격만 남고 인상은 안 따라온다 — 치환 계획에서 이 자리를 먼저 정해야 한다.
ⓑ 한국어 화면 기준 수치이므로 영문 카피로 갈 경우 96/600 히어로의 줄 나눔을 다시 재야 한다.
