---
name: sg-landing-forge
description: 랜딩을 레퍼런스급 품질로 단조한다 — v1 변환 아키텍처. 리드 레퍼런스를 재현급으로 실측(번들→IR)해 골격을 그대로 세우고, 그 위에서 토큰·카피·자산·시그니처를 치환한다. 모든 의무는 기계 게이트(conform 토큰 준수·behavior 동작 검증·detect AI티·잔존 검사)나 전담 에이전트가 판정 — 판정자 없는 규칙은 없다. 시작 시 인터뷰 필수. 트리거 — "랜딩 만들어줘", "랜딩 다시 만들어", "홈/메인 페이지 개편", "랜딩 갈아엎어", "레퍼런스급으로", "landing page 제작". 비대상 — 픽셀 복제 자체는 sg-web-replicate, 문체만 다듬기는 sg-ko-humanize, 사업성 검토는 sg-biz-validate.
---

# sg-landing-forge v1 — 변환 단조

> 이 파일 하나가 Claude Code와 Codex 양쪽에서 동작한다. 각 페이즈는 **읽는 파일 → 쓰는 파일** 계약으로 정의되며, 서브에이전트는 실행 최적화일 뿐이다(폴백은 "런타임 중립" 절).
> 검수·채점·스키마는 `references/forge-rules.md` · Tier-S 요약 라이브러리 `references/library/` · Tier-R 번들 캐시 `references/bundles/`(**로컬 전용, 커밋 금지**) · 방향 추첨 `references/directions.md`.

## 세계관 — 왜 변환인가

구세대(생성 아키텍처)의 5회 실전은 전부 실패했다. 원인은 두 겹이다: **산문 규칙은 실행 에이전트를 구속하지 못하고**(토큰 13개를 선언한 런이 hex 32개를 썼다), **손으로 쓴 스펙은 페이지의 수천 미시 결정을 다 담지 못한다.** 그리고 결정적 비대칭 — **생성에서 스펙의 침묵은 LLM 사전분포로 채워지고(=AI 티), 변환에서 침묵은 레퍼런스 실측값으로 채워진다(=품질 바닥이 상속된다).** 그래서 v1은:

1. **골격은 코드로 차용, 표현은 측정 가능하게 다르게** — 리드 레퍼런스를 재현급으로 실측(번들)하고 IR로 증류해, 그리드·리듬·모션·인터랙션 구조가 이미 살아있는 골격 위에서 시작한다. 빌더의 일은 창조가 아니라 **전사+치환**이다. 팔레트·타이포·카피·자산·시그니처는 의무 치환 — 잔존은 기계가 잡는다.
2. **모든 의무에 판정자** — 기계 게이트가 판정하거나, 전담 에이전트의 유일한 임무거나, 규칙이 아니다. 판정자를 명시할 수 없는 규칙은 이 스킬에 추가할 수 없다(아래 강제 지도).
3. **증거는 디자인할 수 없다** — 자산 등급이 페이지 등급의 상한. 가짜 증거(생성 얼굴·후기)는 이 스킬 최악의 사고.
4. **정직 고지** — 변환은 바닥을 리드 수준으로 끌어올리지만 천장(치환 취향의 격)은 AD·패널 몫이다. 번들 확보 실패 시 생성 폴백은 등급 캡 B. 스탬프 없는 첫 리드는 수집 시간이 든다.

## 강제 지도 (enforcement map) — 의무 → 판정자

| 의무 | 판정자 | 시점 |
|---|---|---|
| 색·폰트·radius·duration은 tokens.json 어휘만 | `conform.mjs` 소스 린트(빌드 직후) + 렌더 검증(뷰포트 1440/390) | P5·P6 |
| 토큰 어휘 예산(중립≤6·브랜드≤2·시맨틱≤2·duration≤2·easing≤1)·변조 방지(해시) | `conform.mjs` 자체 린트 + `--expect-hash` | P2·P6 |
| 엔지니어드 모먼트 실재(하한 3: 히어로 인터랙티브+스크롤 연출+시그니처 마이크로) | `behavior.mjs` — engineering.json 선언을 실행·검증(선언은 AD, 검증은 기계 — 억지 인터랙션 인센티브 차단) | P6 |
| no-JS·reduced-motion에서 콘텐츠 가시 | `behavior.mjs` 가시성 배터리 | P6 |
| AI 티·품질 결함·한글 조판 | `detect.mjs`(면제 반영, 🔴 0·QF 0) — 번호 텍스트 목록(IC6)·서술형 스탯 밴드(DE3) 포함 | P6 |
| 골격 수치가 IR 오차 밴드 내 | 자기 diff(`capture.mjs` ↔ ir.json) | P6 |
| 리드 표현 잔존 0(팔레트·CSS 리터럴·자산 해시) | 잔존 검사 — conform(리드 색은 토큰에 없으므로 자동 위반) + verbatim grep + 해시 대조 | P6 |
| 리드로 오인되지 않을 것 | 패널 divergence 판정("이 페이지의 원본 레퍼런스를 특정할 수 있는가" — 특정되면 실패) | P7 |
| 이미지 실물감·인슬롯 핏·시장 정합 | 게이트키퍼(신선한 컨텍스트, 인슬롯 렌더 캡처로 판정) | P3 |
| 가짜 증거 금지(E클래스 생성 불가) | 자산 스프린트 클래스 규칙 + 게이트키퍼 | P3 |
| 카피는 facts 1:1 | 카피 검수(facts 대조표 의무) | P4·P7 |
| 같은 형태 반복 금지 | history 대조(팔레트 계열+7축) + **리드 회전**(동일 리드 3회 내 재사용 금지, 사용자 지명 예외) | P2 |
| 판정자 없는 규칙 추가 금지 | 이 표 자체 — 새 의무는 행 추가로만 | 상시 |

## 인터뷰 — 시작 시 반드시 묻는다 (유일한 질문 단계)

한 번의 질문 배치(Claude=AskUserQuestion, 기타=번호 목록). 이후 무인 완주.

1. **수준** — 완전 신규 / 콘텐츠 유지·디자인만 / 특정 섹션.
2. **방향** — 로고·브랜드를 실제로 본 뒤 후보 3개+추천 1개, 성격이 다른 축 혼합(규율형 toss·linear·jobber / 스펙터클형 meshy — 갤러리 자산 제작 전제 명시).
3. **자산 정책** — 실캡처·실사진 중심 / 생성 적극 / 혼합(추천). 직전 자산 재사용은 명시 허락제.
4. **증거 인벤토리** — 실고객·후기·현장 사진·수치가 있는가. 없으면 "상한 B + 촬영 숙제 발행"을 여기서 고지.
5. **리드 레퍼런스** — 골격을 소유할 1개(+섹션 graft용 세컨더리 후보). **Tier-R 번들 유무를 확인하고, 없으면 수집 소요를 고지.** 사용자가 URL을 지목하면 그것이 리드다.

## 공정 — P0~P9 (읽는 파일 → 쓰는 파일)

- **P0 인터뷰·계약** — 위 5문항 → `_forge/contract.md`. PRD·실제 제품에서 사실 전수 추출 → `_forge/facts.md` + 없는 증거는 `_forge/homework.md`(샷리스트 브리프)와 등급 캡 사유.
- **P1 리드 번들 확보** — 캐시(`references/bundles/<리드>/`) 있으면 스킵. 없으면 `bundle.mjs`(DOM+computed 트리·CSS·@keyframes·자산 매니페스트) + `record.mjs`(호버·리빌 타임라인·리스너 인벤토리·실행 애니메이션) + `probe.mjs` → `ir.mjs`로 `ir.json` 증류(섹션 기하·리듬·타입 롤·자산 슬롯·모션 어휘). **재현 시험 체크리스트**(forge-rules §8b) 통과 확인 — 스탬프 없는 IR은 리드 자격 없음. 번들 확보 불가(로그인 벽 등) 시 생성 폴백 선언+**등급 캡 B 고지**. WebGL·복합 위젯은 "재현 불가 목록"에 정직하게 기록하고 분석 에이전트가 조작하며 파라미터를 기술로 남긴다.
- **P2 아트디렉터 = 치환 설계자** — 읽기: contract·facts·ir.json·history. "무엇을 지을까"가 아니라 **"무엇을 바꿀까"의 결정자.** 쓰기: ① `DESIGN.md`+`_forge/tokens.json`(스키마 forge-rules §8a — 자기 팔레트·타입, **리드와 팔레트 계열 상이 의무**, 어휘 예산 린트 `conform.mjs --tokens`로 즉석 확인, 승인 해시를 provenance에 기록) ② `_forge/engineering.json`(번들 record/probe에서 **자동 시드** 후 유지/교체/추가 선언 — 하한 3, 역할 커버) ③ `_forge/mapping.md`(리드 섹션→우리 섹션: 유지/삭제/삽입/graft, 아날로그 없는 섹션은 생성 지정+스펙) ④ **시그니처 인벤토리→치환 계획**(번들에서 리드 고유 표현 전수 목록화 — 각각 무엇으로 대체하는가) ⑤ `_forge/provenance.md`(transform ledger: 차용 구조·제거 시그니처 전수) ⑥ `_forge/exemptions.md`(§0 — QF·KO 면제 불가) ⑦ history 대조(리드 회전 포함).
- **P3 병렬: 자산 ∥ 카피** — 자산 스프린트: **아이콘 세트 포함**(라벨·카테고리에 시각 앵커가 필요하면 imagen 투명 생성→trim→캔버스 정규화→토큰 색→인슬롯 확인 — forge-rules §2d, 의미 결합 없는 장식은 여전히 금지), 슬롯 명세는 **ir.json의 실측 슬롯에서 자동 도출**(치수·종횡비·object-fit — 슬롯 우선 철칙·인슬롯 핏 검증·3회 실패 시 슬롯 재설계 콜백 유지), E/P/I 클래스 규칙·시장 정합·성능 예산(히어로 ≤200KB webp)·og:image/파비콘, 완료 시 게이트키퍼(인슬롯 렌더 캡처 판정). ∥ 카피: facts 1:1, sg-ko-humanize 금지 목록, 소리 내어 읽기 테스트, 가격은 숫자로.
- **P4 브리프 컴파일** — 섹션별 `_forge/briefs/<n>.md` = **verbatim 병합**(요약 금지): IR 슬라이스(해당 섹션 수치 원문) + 토큰 치환표(리드값→자기 토큰) + engineering 항목 + 금지 목록(면제 반영) + 수용 기준(어느 게이트가 무엇을 재는가) + 자산 파일 경로. **완전성 린트**: IR 슬라이스의 모든 수치가 브리프에 등장하는지 기계 대조.
- **P5 변환 빌드** — 빌더는 **브리프 하나만 읽는다.** IR 골격→프로젝트 실제 스택 코드로 1:1 전사(**창조 금지, 전사+치환만**), 동작은 engineering.json대로 구현. 섹션 완료 즉시 `conform.mjs --lint`(raw 색·font-size 리터럴 0).
- **P6 기계 배터리** — 단일 브라우저 세션에서 일괄(재실행 저렴): `detect.mjs`(🔴 0·QF 0) + `conform.mjs --url`(토큰 준수·리드 색 잔존 자동 검출·해시 대조) + `behavior.mjs`(engineering.json 전 항목 실측 + no-JS/reduced-motion 가시성) + 자기 diff(capture ↔ ir.json, 밴드 §6b) + **잔존 verbatim 0**(리드 CSS 리터럴·자산 해시 grep) + 성능 예산 + 프로젝트 표준(type-check). 결과 `_forge/qa/`.
- **P7 패널** — 신선한 컨텍스트: ① 패리티(같은 골격이므로 공정한 비교 — 번들 캡처와 5축 절대 채점, §7a) ② 페르소나 5문항(§7b) ③ **divergence 판정**("원본 레퍼런스를 특정할 수 있는가" — 특정되면 실패, 신종 사고 '덜 치환된 클론'의 방어선).
- **P8 라운드 2 (기본 포함)** — 지목 섹션 한정 정밀 수리 → P6 재실행.
- **P9 배포·보고·기록** — og/파비콘/성능/리다이렉트 → 보고서(섹션 캡처, facts→카피 대응, transform ledger, 게이트·패널 수치, 숙제·캡 사유) → history.json append(**리드 사용 기록 포함**).

## 계기 (scripts/ — 순수 node+playwright, 양 런타임 공통)

| 계기 | 역할 |
|---|---|
| `bundle.mjs` | 리드 정적 수집: DOM+computed 트리(1440/390)·CSS 원문·@keyframes·자산 매니페스트·폰트 — **로컬 캐시 전용, 커밋 금지** |
| `record.mjs` | 리드 동적 수집: rAF 타임라인(호버·리빌, 오버슈트=스프링 감지)·CDP 리스너 인벤토리·getAnimations |
| `ir.mjs` | 번들→IR 증류(커밋 가능한 측정 사실): 섹션 기하·리듬·타입·슬롯·모션 어휘. 재현 스탬프 필드 |
| `conform.mjs` | 토큰 준수: 소스 린트+렌더 전수 검사, 어휘 예산, 해시 대조. alpha 정책 rgb-in-set-alpha-free |
| `behavior.mjs` | engineering.json 선언 실측 검증 + no-JS/reduced-motion 가시성 |
| `detect.mjs` / `probe.mjs` | AI 티 37룰 / 호버·리빌·모션 실측 (구세대에서 계승) |
| `capture.mjs` (sg-web-replicate) | 자기 diff 측정 |

## 라이브러리 — 2단

- **Tier-S** (`references/library/`, 18종+): 요약 실측 문서 — 방향 인터뷰·문법 교차 결론의 근거. 커밋 대상.
- **Tier-R** (`references/bundles/<이름>/`): 재현급 번들 — 리드로 지목될 때 **온디맨드 수집**, 로컬 캐시로 누적. **원본 CSS·자산 사본이므로 공개 저장소 커밋은 재배포다 — 절대 금지(gitignore).** 커밋 가능한 것은 ir.json(측정 사실)과 요약뿐. 캐시 소실은 수집 스크립트로 복구 가능.

## 런타임 중립 폴백

서브에이전트 없는 환경: 페이즈 순차 실행하되 **다음 페이즈는 기억이 아니라 파일만 읽는다.** 평가 페이즈는 새 세션 권장. 이 근사로는 산출물 한 등급 하향을 보고서에 정직하게 쓴다.

## 자산 생성 파이프라인 (I클래스 전용)

- **0단계 — 슬롯 읽기**: ir.json 슬롯 명세(치수·종횡비·안전영역·주변 배경) 없이 생성 금지. 프롬프트에 종횡비·구도 명시, 다른 비율 생성 후 우겨넣기 금지.
- Codex `$imagen` / Claude Code는 `codex` CLI imagen / 둘 다 불가 시 "미달성" 보고(SVG 즉석 작도로 때우지 않는다 — §SVG 정책).
- 아이콘: 투명 생성 → `magick -trim +repage` → 캔버스 정규화. **E클래스 생성 절대 금지, P클래스는 픽스처 시딩 후 실캡처만.**

## 후속 명령

- "이 섹션만 다시" — 해당 브리프부터 P5~P6 · "방향 바꿔" — history append 후 P2부터 · "리드 바꿔" — P1부터 · "숙제 자산 왔어" — 슬롯 교체 후 P6~P7 · "라이브러리 추가: <URL>" — benchmarks.md 절차

## 사전 준비 (첫 실행 시 1회)

```
npm i -D playwright && npx playwright install chromium
```

## 옵션

- `수준: 신규|디자인만|섹션 <이름>` / `방향: <이름>` / `리드: <라이브러리 문서명|URL>` / `로케일: ko|en|전체`
