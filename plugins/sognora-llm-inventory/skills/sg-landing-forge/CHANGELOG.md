# sg-landing-forge — CHANGELOG

## 1.6.3 — 2026-08-19 (DE4 신설 — "여백이 콘텐츠보다 큰 밴드"가 게이트를 통째로 빠져나간 사고)

실전 사고: 4열 구분선 밴드(01 현장 사진 / 02 도장면 / 03 색상안 / 04 고객 검토)가 박스 209px에 콘텐츠 59px, 상하 공백 150px로 출하됐다. 재현 페이지로 확인한 결과 **기존 룰 넷이 각각 조건 하나씩 차이로 전부 빗나갔다**:

- CP2(번호 뱃지) — 번호가 칩이 아니라 맨 텍스트여서 `hasOwnBg` 미충족
- IC6(번호 목록) — 번호를 **칼럼 축으로만** 묶어서 가로 스테퍼는 각 1개씩으로 흩어짐
- LA1·DE2(저밀도 카드) — 열에 배경·그림자가 없어 `surface` 미충족
- DE1(여백 지배 섹션) — 섹션 높이 451px < 0.6vh 문턱
- DE3 — 열 텍스트가 "01"로 시작해 `numText`로 분류되어 제외

5열로 늘리면 **탐지 0건**으로 완전 탈출하는 것까지 재현 확인.

- **DE4(🔴, 신규)**: 아이템 박스의 상하 공백 합 > 안쪽 콘텐츠 높이면 위반. 분모를 섹션이 아니라 **아이템 박스**로 잡은 것이 핵심 — 섹션 패딩(96~120px)은 리듬이므로 무죄, 죄는 카드·열 안쪽이 내용을 압도하는 것. 두 갈래로 판정: ⓐ 행 멤버 과반이 자기 박스의 절반도 못 채움(하한 120px·공백 64px) ⓑ 경계(배경·보더) 있는 밴드의 잉크가 자기 높이의 절반 미만(하한 140px·공백 96px)
- **잉크 박스 계측 신설**: `childInfo`가 박스 안에서 글자·미디어가 실제로 찍힌 세로 구간(`inkTop`·`inkH`)을 잰다. 컨테이너에 `surface`·`bordered` 수집 추가
- **IC6 확장**: 칼럼 축에 더해 **행 축**(가로 스테퍼, x 분포 ≥ 섹션 폭 30%)을 본다. 서수 판정을 `isOrdinal()`로 통일해 "STEP 01"·"단계 1"·"1." 표기도 포함
- **rules-lib**: `rowGroups()`(멤버 반환, `rowsOf`는 이를 위임)·`isVoidBox()`·`bandVoid()`·`VOID` 상수 신설 — 판정 로직은 detect와 gate가 공유
- **처방 문서화**: forge-rules §2d에 "패딩 축소가 아니라 내용 투입이 먼저"(실체 투입 → 한 줄 압축 → 삭제), layout-alternatives에 번호 스테퍼 해체 처방
- **DE4 밴드 분기 축소(오검 1건 반영)**: aside.com 계측 중 `border-x … py-24` 레일(아이브로+제목+본문 세로 스택, h360/잉크168/공백192)이 잡혔다. 세로로 쌓인 섹션 헤더는 같은 수치라도 정상 리듬이므로, 밴드 분기에 **다열(가로 ≥3) 요구**를 추가. 재검증에서 aside.com DE4 0건 · 재현 4변형은 그대로 검출
- **레퍼런스 신규**: `references/library/aside-com.md`(Tier-S) — 계측 부산물이 그대로 라이브러리 자산이 됐다. INDEX 등록 + 교차 결론 7번 신설

**감사 라운드 반영 (fable 감사자 실측 근거)**

- **DE4가 4가지로 재탈출하던 것 차단**: ⓐ LA2·DE3에 남아 있던 `≤4` 상한 제거(제가 DE4만 고치고 자매 룰을 안 고쳤다 — 5열이면 셋 다 침묵했다) ⓑ 잉크를 요소 rect가 아니라 **텍스트 노드 Range의 구간 합**으로(맨 텍스트 노드·상하 각주로 스팬만 채우는 우회 차단) ⓒ IC6 미디어 사면을 실미디어 기준(10,000px²)으로(90×60 더미 1장이 섹션 전체를 사면했다) ⓓ 투명 잉크 필러 차단(`opacity:.011` 180px 마침표로 DE4를 침묵시키는 우회 — 누적 opacity·글자색 알파 검사)
- **레퍼런스 오탐 제거(toss 4→1, aside 4→0)**: 핀 고정 스크롤텔링 제외 · `background-image` 포토 카드를 잉크로 · **CSS로 그린 목업(그라데이션 서피스·스켈레톤 바)도 잉크로** · 미디어가 박스 30%+면 void 아님. **되돌리지 않은 이유**: 스팬 버전을 복원해 대조 실측한 결과 그것도 toss 2건을 냈다 — revert는 우회만 되살리고 오탐은 남긴다.
- **`bandVoid` 기하 수정** — `inkH`가 '합'이 된 뒤 `inkTop+inkH`를 하단 좌표로 쓰던 산술이 깨져 있었다. `inkMin`/`inkMax`를 따로 반환.
- **QF4 한글 전면 면제 제거 + 행간 프롱 상한**: `!e.korean` 삭제(한글 랜딩 스킬에서 한글이 타이포 하한을 면제받는 건 규칙의 자기부정). 단 행간 프롱은 **본문 크기 ≤20px에만** — 상한이 없어 디스플레이의 정상 행간(1.05~1.2)을 잡던 오탐이 aside에서 4건 났다.
- **QF 이원화**: 차단(QF1·2·4·5 — 콘텐츠·조작 파손) / 보고(QF3·6). **QF6은 속성 프록시라 CSS로 치수 고정한 이미지를 오탐**하므로 바닥으로 승격하지 않는다.
- **audit이 QF를 통과시키던 것 수정** — 통과 조건이 "🔴 0 · QF 0"인데 red만 봤다.
- **재현 스탬프 제도 격하** — 커밋된 IR 16종 **전부에서 `reproduction` 필드가 사라져 있었다**(스키마 진화). 제도가 사문화된 채 "리드 자격 없음"만 선언 중이었다. `completeness` 기계 스탬프 + 자기 diff 게이트 2층으로 대체.
- **스테일 포인터 정리**: `gate.mjs`·`plan.md`·`borrow.md`·`craft-rules.md`·`direction.md` 제거, `_forge/history.json` → `references/history.json` 통일(반복 금지는 프로젝트를 가로지르는 제약이므로 스킬이 기억해야 한다), 룰 ID SSOT를 rules-lib으로 자기선언.

- **`isColumnRow` 신설 — "열"의 정의를 기하로 명시.** row 멤버는 **가로로 서로소**여야 한다(x-겹침 ≤ min(w)의 20%). `rowGroups`가 y-겹침만 봐서 "나란한 3열"과 **"같은 자리에 포개진 3층"**(크로스페이드 스크롤텔링·캐러셀의 표준 구조)을 구분하지 못했다 — toss 실측: x=0·w=1440 전폭 absolute 레이어 3장이 "3열 여백 지배"로 잡혔다. **임계 조정이 아니라 정의 명시**이고, 이걸로 toss DE4가 0이 됐다(4→1→0).
- **MO1에 WAAPI/JS 경로 추가** — `document.getAnimations()` 병용. CSS animation만 보면 **framer-motion·GSAP 같은 JS 구동 루프가 통째로 안 잡혔다**(현대 스택의 기본값). detect 컨텍스트가 `reducedMotion:"reduce"`라 JS 루프가 아예 안 도는 문제도 있어, 스냅샷 직전에만 해제하고 복원한다.
- **`inkBox`를 rules-lib 단일 소스로** — detect가 문자열 주입으로 쓰고 page-craft `alive.mjs`도 같은 소스를 쓴다. 이 함수는 하루 사이 두 번 고쳐졌는데(요소 rect → Range 구간 합 → 투명 필러·배경이미지 대응) 복제본이 있었으면 갈라졌을 것이다.
- **behavior 게이트 자가 무력화 차단**: 하한 3·역할 3종을 **게이트가 하드코딩 소유**(스펙은 올릴 수만 있음). `{"minCount":0}` 한 줄로 "✔ 통과"가 나오던 구조 — 스펙 작성자와 통과 희망자가 같은 에이전트였다. no-JS 검사를 "숨은 텍스트 찾기"에서 **"JS-on 대비 가시 텍스트 총량 비교"**로(콘텐츠가 아예 안 그려지는 최악의 실패가 통과하던 것). `td !== 0` 면제 삭제(§2c "0ms 금지"와 코드가 정반대였다).
- 검증: 재현 4변형(4열·번호없음·5열·STEP 라벨) 전부 🔴 DE4 검출, 실미디어를 이고 여백이 콘텐츠보다 작은 대조군은 미검출. 실제 앱 3페이지(홈·요금제·제품) + aside.com 회귀 스캔에서 DE4·IC6 오검 0건

## 1.2.2 — 2026-08-17 (카피 시점 룰 — "제작 후기·설명서가 랜딩에 실린" 사고)

실전 사고: 본문에 "…짧게 이어 붙였습니다. 자막과 버튼은 잘리지 않는 자리에 놓았습니다" — 만든 사람이 자기 작업을 보고하는 문장이 카피 자리에 실렸다.

- **KO5(🔴 제작자 시점)**: 제작 서술 동사(이어 붙였·배치했·놓았·구성했·반영했·촬영했 등) 2곳 이상 = 위반. 제작 방식·규격 준수 사실은 보고서에 쓰고 페이지에 쓰지 않는다
- **KO6(🔴 설명서 시점)**: "~하시면 됩니다·입력하세요·다음 단계" 류 절차 문장 3곳 이상 = 위반(§2d 매뉴얼화 금지의 문장 단위 검출)
- forge-rules §2c-2 신설: 카피의 주어는 제작자가 아니라 고객·산출물. 검증법 — 문장 앞에 "우리가"가 붙으면 제작 노트, "당신은"이 붙으면 카피

## 1.3.0 — 2026-08-17 (e2e 판정 실행체 — audit.mjs + 패널 브리프 파일)

지금까지 게이트는 스크립트 5개로 흩어져 있어 실행 에이전트가 일부만 돌리고 "통과"를 선언할 수 있었고, 패널 프롬프트는 매번 즉석 작성이라 기준이 흔들렸다.

- **`scripts/audit.mjs`**: 캡처+detect+conform(소스·렌더)+behavior+장면 예산+깊이 대조를 한 명령으로. `audit.md` 판정표 + 패널 입력 캡처 생성. **누락 게이트를 '미제공'으로 명시**(통과로 위장 불가), 종료 코드로 실패 신호. 실측 검증: 3010에서 detect 🔴3 실패·미제공 4건 정확 보고
- **`references/panel-prompts.md`**: §A AI-티 / §B 매력 / §C 페르소나 / §D 게이트키퍼 브리프를 문면 그대로 재사용하는 파일. 치환 변수와 실행 순서 포함
- **§7z 판정 실행 규약**: 기계 축은 audit.mjs 한 명령, 사람 축은 브리프 문면 그대로, 기계 실패 해소 후 패널. 미제공 게이트 존재 시 A등급 불가(B로 캡)

## 1.2.1 — 2026-08-17 (AI-티 판정 실전 발견 3건 추가)

- **동어반복 금지(카피×아이콘)**: 같은 어미·명사로 끝나는 항목 나열 + 제네릭 시스템 아이콘 세트 = 이중 위반("가장 AI스러운 프레임"으로 지목된 패턴). 아이콘 섹션 간 재사용도 금지
- **반폭 공백 금지**: 카드·밴드 우측이 통째로 비는 배치(실측: 1270px 카드에 435px 공백) — 여백은 리듬이어야 하고 미완성으로 읽히면 실패
- **히어로 카피 복제 금지**: 최종 CTA가 히어로 헤드라인·CTA를 어미까지 되풀이하는 것 금지

## 1.2.0 — 2026-08-17 (블라인드 4인 평가 실전 결과 규칙화 — 3010: 54점, 3006: 44점 불통과)

세 판정자가 독립적으로 같은 실패를 지목: 히어로에 산출물이 없고, 절차도가 첫 장면을 차지하고, 면책·미제공 고지가 반복되고, 스톡 사진 스트립을 산출물로 세었다.

- **첫 화면 산출물 하한**: 히어로에 완성 산출물 실물 최소 1점(다양성이 핵심 가치면 벽 형태). 큰 글씨+CTA만, 절차도·스테퍼가 주인공, **미완성 "입력물" 사진**(예: 아직 칠하지 않은 건물)을 히어로에 두는 것 전부 실패 — 히어로는 애프터를 판다
- **면책·부정 고지 총량 제한**: 면책 문구는 페이지 1회, 히어로 배치 금지(볼 것을 보여주기 전에 불신 심기 금지). 미제공 고지도 1곳
- **번호 체계 단일화**: 단계 번호 중간 리셋 금지
- **소재는 산출물이 아니다**: 스톡·생성 제품 사진 스트립은 산출물 전시로 인정하지 않으며 갤러리 다양성 계수에서 제외 — 게재 크롬(광고 배지·도메인·CTA·영상 길이·플랫폼 UI)이 붙어야 산출물

## 1.1.2 — 2026-08-17 (블라인드 평가 실전 발견 2건 규칙화)

- **무라벨 금지**: 제품이 등장하는 생성 소재에 라벨·브랜드 마크가 없으면 실패(생성 이미지의 최대 관용구 — 광고할 제품에 이름이 없는 일은 실무에 없다). 가상 브랜드라도 이름·로고를 정해 라벨을 붙여 생성/합성. 소재 색이 페이지 브랜드색과 정확히 일치하는 것도 프롬프트 흔적
- **동일 시안 반복 금지**: 같은 헤드라인·설명문·피사체의 산출물이 두 섹션 이상 그대로 재등장하면 위반 — 연속성이 필요하면 같은 캠페인의 다른 변형을 쓴다

## 1.1.1 — 2026-08-17

- §2d 중심 장면의 CTA 예시를 제품 무관 형식("무료로 <제품의 산출물> 만들기")으로 파라미터화 — 규칙이 특정 사례(광고 서비스) 전용으로 읽히지 않도록

## 1.1.0 — 2026-08-17 (욕망 축 신설 — "출하 가능인데 44/100" 사고 대응)

실전 사고: 전 게이트 통과 페이지가 블라인드 디자이너 평가 44/100(경쟁 82·92). 완성도 게이트는 욕망을 재지 않았고, 깊이 하한은 절차 설명을 늘리는 매뉴얼화로 오작동.

- **§7d 매력 채점**: 블라인드 디자이너 2인(아트디렉션 절대 /100 + 경쟁 2종 상대), 통과선 절대 ≥70·경쟁 −20 이내. 미달 시 섹션 수리가 아니라 **중심 장면·방향 재설계(P2 회귀)**. A등급 조건 포함
- **깊이의 단위 교정**: 깊이는 절차가 아니라 장면 — 사용 절차·규격·권한은 압축 모듈 1~2개로 제한, 깊이는 갖고 싶은 산출물·증거·장면의 다양성으로(매뉴얼화 금지)
- **중심 장면 1문장 의무(contract.md)** — 히어로는 결과를 판다, 입력 작업("등록하세요")을 파는 히어로·CTA 금지
- **장면 예산**: 동일 배경·레이아웃 시그니처 연속 3섹션 = 위반(P6 기계 검사) — "9,500px 내내 같은 화면" 방어선
- **예시 갤러리 다양성**: 서로 다른 피사체·업종 ≥4(한 사례 매체별 돌려쓰기 금지)
- **정직 고지 집약**: "베타·준비 중"은 전용 자리 1곳(반복 노출 = 미완성 인상)

## 1.0.7 — 2026-08-17 (차단 페이지 쓰레기 IR 제거)

- ohou-se·mayoclinic-org의 IR 삭제 — 차단 페이지(Akamai/403) 캡처를 증류한 무의미 데이터(섹션 0). 재생성 루프가 되살리지 않도록 유의: 차단 판정 번들은 IR 생성 대상이 아니다

## 1.0.6 — 2026-08-17 (폴더 구조 정리 — IR을 1급 커밋 디렉터리로)

- 커밋되는 측정 사실을 `references/ir/<이름>.json`으로 분리(16건 이동) — gitignore 부정 패턴 3줄 의존 제거, 번들은 한 줄로 전체 무시
- 비심링크(마켓플레이스) 설치의 번들 캐시 소실 조건과 복구 경로를 SKILL에 명시(ir.json은 플러그인과 함께 배포됨)

## 1.0.5 — 2026-08-17 (깊이 하한 + 산출물 리얼리즘 — "얕은 페이지·가짜 광고 시안" 실전 사고 대응)

- **깊이 하한**: facts.md에 제품 인벤토리 절 의무(화면·플로우·산출물 실규격 전수 — 깊이는 제품 실체에서 나온다) + P6 깊이 대조(스크롤 길이·섹션 수 vs 리드 IR, -40% 초과 축소는 mapping 삭제 사유 전수 기재 필수). 콘텐츠 예산(부풀리기 금지)의 반대 방향 규칙
- **산출물 리얼리즘**: 제품 결과물 전시는 실제 게재 매체의 실규격·실UI 안에 조립(검색 광고→검색 결과 UI, 배너→IAB 규격). 문구를 이미지에 굽지 않는다 — 텍스트·UI는 HTML/CSS, imagen은 사진 소재까지만. 게이트키퍼 목업 진위감으로 판정

## 1.0.4 — 2026-08-17 (AI-티 시각 채점 + 철거 규칙)

- forge-rules §7c: 마지막 관문 — 블라인드 판정자가 섹션별 스크린샷만 보고 AI-스러움 역척도 1~5 채점. **≤3점 = 해당 섹션 완전 삭제 후 재단조(수리·미세조정 금지)**, 재단조는 치환 전략 최소 1축 교체 의무(앵커/graft/카피 각도), 2연속 철거 시 AD 콜백(정직한 생략 또는 재설계). 통과선 전 섹션 ≥4, A등급 조건 포함
- P7 ④ 채점 단계·P8 철거/수리 이원화·enforcement map 행 추가

## 1.0.3 — 2026-08-17 (시각 앵커·콘텐츠 예산 — "번호 목록 카드·서술형 스탯 밴드·여백 부풀리기" 사고 대응)

- detect 신규 룰: **IC6(🔴)** 번호 텍스트 목록 카드(시각 실체를 "01/02/03" 텍스트로 대체, 주변 실미디어 없음) — 실물 페이지에서 정확히 해당 섹션 2곳 검출 확인 · **DE3(🔴 혐의)** 서술형 스탯 밴드(수치·증거·미디어 없는 라벨+상태문 나열)
- forge-rules §2d: 시각 실체는 주석 이미지로 / 스탯 밴드는 측정값만(없으면 생략) / **콘텐츠 예산**(facts 재고가 얇으면 섹션을 줄인다 — 여백+얇은 텍스트 인플레 금지) / 아이콘 양방향 규칙(장식 금지 ↔ 앵커 0 텍스트 벽도 금지, imagen 투명+trim+캔버스 정규화+토큰 색+인슬롯 확인)
- P3 자산 스프린트에 아이콘 세트 산출 명시

## 1.0.2 — 2026-08-17 (부유 텍스트 귀속의 쓰레기 필터 + A조 최종 정산)

- ir.mjs: 부유 텍스트 귀속에서 cookie/consent/banner/modal류 오버레이 제외 — framer에서 쿠키 동의 문구가 섹션 헤드라인으로 붙던 사고("없음"보다 나쁜 "틀린 값"). 해당 섹션은 이제 정직하게 headline 없음
- A조 최종 정산 반영: toss 히어로 66px 귀속 확정(남은 조건 #10~#12 병합 하나), linear·meshy 무조건 자격, framer·jobber 조건부. 보류 0건

## 1.0.1 — 2026-08-17 (레퍼런스 라이브러리 전면 Tier-R 개편 — 서브에이전트 4조 병렬)

18종 전부 재현급 수집(번들+record+IR)·시각 검증·§8b 체크리스트·재현 노트. 결과: 리드 자격 16종(조건부 포함)·보류 1(stripe — WebGL 시그니처)·차단 2(ohou·mayoclinic). ir.json 16건 커밋(번들 원본은 로컬 캐시 전용). INDEX에 Tier-R 현황 표.

계기 담금질(에이전트 실전 발견 → 즉시 수리 5라운드):
- ir.mjs: 오버레이·커튼·배경 아트 섹션 제외+layers 필드, 래퍼 분할 유니온 가드(중첩 이중계산 수정), **헤드라인 가드**(자식들이 각자 제목을 가질 때만 분할 — 제목/본문 찢김 방지), 잎 미디어 후보 제외, **부유 텍스트 기하 귀속**(절대배치 히어로 카피), 폭 0.75·깊이 14·maxH 5500·컨테이너 여백 ≥24px·같은-y 중복 제거
- bundle.mjs: body 자식 절단이 script류에 밀려 콘텐츠를 날리던 버그, 표준 UA(jobber CDN 차단 해소), 전체 페이지 샷+밴드 샷(fullPage 하단 백지 결손 대비), 트리 깊이 22
- record.mjs: 표준 UA, 자동 셀렉터 내비·배너 제외+y 분산 표집

## 1.0.0 — 2026-08-17 (아키텍처 전환: 생성 → 변환. 하브루타 3라운드 합의)

실전 5회 전패 + "그대로 카피할 만큼의 정보가 없다"는 지적으로 전면 재설계. 진단 두 겹: 산문 규칙은 실행 에이전트를 구속하지 못하고(토큰 13개 선언한 런이 hex 32개 사용, keyframes 0), 손 스펙은 페이지의 수천 미시 결정을 못 담는다 — 생성에서 스펙의 침묵은 LLM 사전분포(=AI 티)로, 변환에서는 레퍼런스 실측값으로 채워진다.

- **변환 아키텍처(P0~P9)**: 리드를 재현급 실측(번들→IR)해 골격을 세우고 토큰·카피·자산·시그니처를 치환. 빌더의 일은 창조가 아니라 전사+치환
- **계기 5종 신설·검증**: `bundle.mjs`(DOM+computed 트리·CSS·@keyframes 99개 수확 확인·자산 매니페스트 — 로컬 캐시 전용·커밋 금지), `record.mjs`(rAF 타임라인·오버슈트 감지·CDP 리스너 인벤토리), `ir.mjs`(커밋 가능한 측정 사실 증류 — meshy 9섹션 분할 검증), `conform.mjs`(토큰 준수: 소스 린트+렌더 전수, alpha 정책·어휘 예산·해시 변조 방지 — nolpop 탄생 테스트에서 위반 10종 검출), `behavior.mjs`(engineering.json 선언→실측 검증 + no-JS/reduced-motion 가시성)
- **강제 지도(enforcement map)**: 모든 의무에 판정자 명시 — 판정자 없는 규칙은 추가 불가
- **잔존·divergence 게이트**: 신종 사고 "덜 치환된 클론" 방어 — 리드 팔레트·verbatim·자산 해시 잔존 0(기계) + "원본을 특정할 수 있는가" 판정(패널)
- **2단 라이브러리**: Tier-S 요약 18종 유지, Tier-R 번들은 리드 지목 시 온디맨드 수집·로컬 캐시(공개 저장소 커밋은 재배포 — 절대 금지)
- **정직 고지**: 변환은 바닥을 사주지 천장을 사주지 않는다(치환 취향은 AD·패널 몫), 번들 확보 불가 시 생성 폴백은 등급 캡 B, 리드 회전(3회 내 재사용 금지)

## 0.18.1 — 2026-08-17 (슬롯 우선 — "이미지가 지맘대로" 사고 대응)

실전 사고: 이미지 생성이 들어갈 자리의 크기·종횡비를 모른 채 돌아 슬롯과 안 맞는 이상한 자산이 반복 생산됨. 수정:

- **섹션 스펙의 자산 슬롯을 완전 명세로**: 렌더 치수(데스크톱·모바일)·종횡비·object-fit·라운드·주변 배경색·투명 여부·오버레이 안전영역
- **슬롯 우선 철칙**: 슬롯 명세 없으면 생성 없다. 생성 해상도 = 슬롯 ×2, 종횡비 고정, 프롬프트에 구도 명시
- **인슬롯 핏 검증 루프**: 실제 슬롯에 렌더→캡처→확인(레터박스 0·피사체 크롭 훼손 0·톤 정합), 3회 실패 시 슬롯 재설계 콜백
- **게이트키퍼 입력을 인슬롯 렌더 캡처로 변경** — 사람은 이미지를 페이지 안에서 본다

## 0.18.0 — 2026-08-17 ("정적 HTML 끄적임" 사고 대응 — 엔지니어링 계약 신설)

실전 사고: 라이브러리의 인터랙션·JS 실측(framer 스프링 175건, toss IO 리빌, meshy 유리+글로우, jobber mask 전환)이 "읽을거리"로만 존재하고 빌드 계약에 없어, 정적 섹션만 쌓아도 전 게이트 통과. 수정:

- **engineering.md 필수 산출물(§2⑦)** — 엔지니어드 컴포넌트 인벤토리, **하한 3개**(히어로 인터랙티브 + 스크롤 연출 + 시그니처 마이크로 인터랙션), 각각 라이브러리 출처·상태 전이표·수용 기준
- **동작 증거 게이트(§7)** — 항목별 probe 수치 또는 조작 전후 캡처 쌍 없이 완료 선언 불가. A등급 조건에 포함
- 빌드 산출물 정의 강화 — 프로젝트 실제 스택의 컴포넌트 코드만 인정, 일회용 정적 HTML 불인정. 개폐류 0ms 금지(120~150ms 하한)
- 세계관에 명문화: "랜딩은 소프트웨어다 — 레퍼런스의 절반은 엔지니어링이다"

## 0.1.2 — 2026-08-17 ("왜 meshy가 안 나오나" 대응)

- **방향 면제 조항 신설(forge-rules §0)** — meshy 실측(🔴 8개인데 산출물 109개가 정당화)이 근거. 방향이 명시 채택한 티는 exemptions.md 선언으로 "채택된 스타일" 처리. 단 QF·KO·AS2·가짜 증거는 면제 불가, 정당화 조건(예: 갤러리 밀도) 미충족 시 무효
- 인터뷰 방향 후보에 성격 축 명시(규율형 vs 스펙터클형) — 실행자가 안전한 에디토리얼로만 수렴하던 편향 해소
- I클래스에 "예시 산출물 갤러리" 명시 허용(생성 가능 — "예시" 정직 라벨 필수, 고객 실적 위장 금지)

## 0.1.1 — 2026-08-16

- **SVG 정책 신설** — 일러스트·다이어그램·시트류의 SVG 즉석 작도 금지(라이브러리 18종 실측: 해당 용례 0건, toss는 다색 아이콘도 4x PNG 선택). 허용은 아이콘 세트·로고·단일 path 마이크로 자산(마스크·밑줄)뿐이며 그것도 렌더→눈 확인 루프 필수. LLM의 blind 좌표 작도가 "이상한 SVG"의 원인

## 0.1.0 — 2026-08-16

- 신설. sg-landing-craft를 폐기하고 하브루타 재설계 — 사용자 판정 "스킬이 레퍼런스 퀄리티를 못 따라간다"의 원인 규명에서 출발:
  - 레퍼런스 퀄리티의 실체 = 성숙한 시스템 + **증거 재고**(디자인 불가·수집만 가능) + 측정된 실행
  - 기존 스킬은 검수 80%/제작 20%였고, 측정과 판정 사이에 빌드를 스펙에 묶는 기계 장치가 없었다
- 10페이즈 공정(파일 계약으로 런타임 중립): 인터뷰(+증거 인벤토리·리드 레퍼런스) → facts+숙제 브리프 → 아트디렉터(단일 결정권·리듬은 리드 1개 소유·섹션 스펙 수치화·provenance) → 자산 스프린트(E/P/I 클래스·픽스처 시딩·이미지 게이트키퍼 전진 배치·성능 예산·og) → 카피(소리 내어 읽기) → 섹션 빌드+**자기 diff 게이트**(스펙이 원본) → AD 일관성 검수 → 계기 검수(모바일 별도 구도) → **패리티 채점**(선호 판정 금지 — 5축 절대 채점, 레퍼런스 -1 이내) → 라운드 2(실패 섹션 정밀 수리) → 배포 체크리스트
- 핵심 금지: 가짜 증거(E클래스 생성) 절대 금지 · 시그니처 복제 금지(숫자만 차용, provenance 감사) · 문서 없으면 코드 없다
- 하브루타에서 기각된 내 초안: side-by-side 블라인드 선호 판정(장르 편향·수렴 압력), 레퍼런스 블렌드 규칙(프랑켄슈타인), 모션 엔지니어 역할, 자산 절대주의(데드락)
- 승계: 라이브러리 18종(측정 캐시로 승격)·detect/probe/rules-lib·directions(history 차별)·benchmarks·페르소나/인터랙션 평가
