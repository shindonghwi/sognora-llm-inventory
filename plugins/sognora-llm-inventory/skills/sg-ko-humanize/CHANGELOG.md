# sg-ko-humanize — CHANGELOG

## 1.6.7 — 2026-08-21 (패턴 검사와 자연스러움 판정을 분리)

- **§9 추상 연결·빈 동사 신설** — `한 곳에서/하나로/자연스럽게/처음부터 끝까지`가 `이어지다/연결되다/함께하다`와 결합해 행동·결과를 가리면 KOH23 🟡 후보로 올린다. `이어집니다` 자체는 금지하지 않아 구체적인 조건·다음 화면 문장은 통과한다.
- **의미 검토 3문항을 필수 단계로 분리** — 사용자가 무엇을 하는지, 무엇을 보거나 얻는지, 빈 동사가 그 답을 가리는지를 정규식 검사 뒤에 확인한다. 기계 exit 0의 보고 범위는 `알려진 패턴 검사 통과. 자연스러움과 문맥 적합성은 별도 검토가 필요함.`으로 제한했다.
- **근거 기반 원칙 확장** — literal 정규식뿐 아니라 rules.md의 의미 검토 기준에 매핑된 구간도 편집할 수 있다. 대신 주변 문맥에 구체 사실이 없으면 새 행동·결과를 지어내지 않고 사람 검토로 남긴다.
- **영어 숫자 단어 정합** — `locale_parity.py`가 영어 0~99 숫자 단어를 상대 로케일의 아라비아 숫자와 대조할 때만 정규화한다. `eight characters`와 `8자`는 일치하고, `eight`와 `7`은 계속 불일치다.
- **짧은 문구의 변경률 예외 제거** — 2-gram 유지율은 보조 경고로만 쓰고, 모든 길이에 30% 초과 WARN·50% 초과 ABORT를 먼저 적용한다.
- **회귀 fixture와 저장소 게이트 추가** — 추상 연결 미탐 2건·정상 대조 3건(구체 문맥의 `이어집니다` 포함)·영어 숫자 단어 일치/불일치·짧은 문구 50% 초과를 stdlib `unittest`로 고정했다. `tools/check.sh`가 이 테스트를 실행하므로 PyYAML 없이도 동작 검증이 반복된다.
- **탐색성 개선** — rules.md 앞에 규칙·게이트·검증 절로 가는 목차를 추가했다.

## 1.6.6 — 2026-08-19 (남아 있던 "사람 몫" 셋을 판정자에게 넘김)

1.6.5까지 문서에 "기계가 못 본다"고 적어둔 축이 셋이었다. 적어두는 것과 막는 것은 다르다 — 셋 다 **결정 가능한 부분이 남아 있었고**, 그만큼만 정직하게 떼어 기계에 넘겼다.

- **KOH22 파일 간 용어 표류** — KOH21은 파일 안만 본다. 실제 사고는 그 바깥에서 났다: 랜딩은 "요금제"로 통일돼 있고 요금제 페이지는 "플랜" 6회였는데, **각 파일이 내부적으로 일관돼서 KOH21이 침묵**했고 사람이 눈으로 찾았다. 파일을 여러 개 넘기면 각 파일의 '지배 용어'(압도적 다수)를 뽑아 서로 다른지 본다.
- **UI 동작 반의어 11쌍** — `change_rate.py`가 "저장"→"삭제"를 통과시키던 문제. 숫자·부정·극성 토큰이 하나도 없어 초단문 카피에는 **불변식이 걸릴 자리가 없었다**. 일반 산문의 반의어는 사전 없이 불가하지만 **버튼·메뉴의 동작 어휘는 닫힌 집합**이라 열거할 수 있다(저장↔삭제·구독↔해지·공개↔비공개·활성화↔비활성화 등). "확인/취소"는 뺐다 — 둘 다 산문에 흔해 오탐원이다.
- **부분문자열 함정 수정** — "비공개"가 "공개"를 포함해서 `공개→비공개` 교체에서 '공개가 사라지지 않은 것'으로 보였다. 접두 부정(비-/미-/불-) 반전이 통째로 빠져나가던 버그로, 상대말의 부분문자열로 들어간 출현을 빼고 센다. 양방향 확인.
- **`locale_parity.py` 신설** — `change_rate.py`는 같은 파일의 before/after만 본다. 한 로케일만 고치면 그 안에서는 아무 불변식도 안 깨진다. 키 누락·숫자·보간 토큰·URL·기술 약어·**응답 극성**(en이 "No."로 답하는데 ko에 부정 표지가 없음)을 본다. 축이 다르므로 별도 계기다.
- **실코퍼스 캘리브레이션 — 첫 구현은 오탐 3종으로 터졌다.** ⓐ 연속 대문자를 전부 약어로 세서 영문 eyebrow("SHARE WITH CLIENTS")가 한글에 대응물 없다고 전부 걸렸다 → 기술 용어 화이트리스트(JPG·PNG·MB·API…)로만 본다. ⓑ `$19 ↔ 19,000원`을 숫자 불일치로 잡았다 → 통화 표지가 걸린 값은 숫자·보간을 비교하지 않는다. ⓒ 로케일 블록이 없는 배럴 파일을 실패로 찍었다 → "해당 없음". 실측 6파일 기준 불일치 14건 → **2건**으로 수렴했고, 남은 2건은 실제 결함 후보다.
- 검증: 실코퍼스에서 **손으로 찾았던 결함을 기계가 재현**했다(landing FAQ의 en "No." ↔ ko 부정 표지 소실). 정합 픽스처 0건 · 결함 픽스처 5종 전수 검출 · 통화 픽스처 0건 · 산문·법률 회귀 무변화 · 장르 미지정 거절 유지.

## 1.6.5 — 2026-08-19 (장르 미지정 거절 + 용어 표류 계량)

1.6.4가 UI 장르를 만들었지만 **부르는 쪽이 `--genre`를 기억해야만** 작동했다. 안 넘기면 예전 그대로 🔴0 exit 0이 나왔다 — 고친 줄 알았던 거짓 통과가 기본 경로에 그대로 남아 있었다.

- **코드·메시지 파일에 장르 미지정 = exit 2 거절.** `.ts/.tsx/.js/.jsx/.mjs/.cjs/.json/.vue/.svelte/.py`가 대상이다. 기본 장르는 큰따옴표 안을 인용으로 지우므로 그 호출의 0은 "깨끗함"이 아니라 "아무것도 안 봄"이었고, 둘이 구분되지 않는 게 이 사고의 뿌리였다. 침묵 대신 거절하는 건 `change_rate.py`의 "미지원 구문 — 수정 금지"와 같은 규약이다. **자체검증 2b에 "exit 2는 통과가 아니다"를 못 박았다.** 산문(.md/.txt)은 장르 없이도 이전처럼 동작한다.
- **KOH21 용어 표류 — "기계가 못 센다"던 축을 부분적으로 계량했다.** 한 파일 안에서 같은 것을 두 이름으로 부르는지 동의어 클러스터로 센다(요금제/플랜, 미리보기/프리뷰, 계정/어카운트, 로그인/사인인). **역할이 갈리는 쌍은 넣지 않았다** — "고객/사용자"는 실측에서 고객=시공사의 의뢰인, 사용자=좌석 수로 서로 다른 것을 가리켰다. 넣었으면 오탐이었다. 표 밖의 표류와 **파일 간 표류**(랜딩은 "요금제", 요금제 페이지는 "플랜")는 여전히 사람 몫이고, 그렇게 적어 둔다.
- **문서 정합** — SKILL.md에 "7대 카테고리"·"`--genre UI`를 넘긴다"·"용어 표류는 기계가 못 센다"가 옛 서술로 남아 있던 것을 실제 동작에 맞췄다. `sg-page-craft`는 "`--type landing`이면 랜딩"이라 적어놓고 명령줄엔 `--genre UI`가 박혀 있던 불일치를 해소.
- 검증: nolpop 메시지 파일 6개 🔴0 수렴(landing 편집 화면·제공량·적용 위치·지원 범위, foundation 작업 화면, pricing 플랜→요금제 통일) · 불변 게이트 3파일 exit 0 · type-check 통과 · 산문·법률·스펙 회귀 무변화.

## 1.6.4 — 2026-08-19 (UI 장르 신설 — 검출기가 제품 카피를 구조적으로 못 보던 사고)

사용자 실측 반려: 랜딩 i18n 파일에 "읽기 전용 화면 / 고객이 결정할 화면 / 편집 화면 / 제공량 / 지원 범위"가 그대로 살아 있는데 `detect_ko.py`가 **🔴0 🟡0 "깨끗함"**을 찍었다. 사람이 두 번 지적해서야 고쳐진 카피를 기계는 만점으로 통과시켰다. 원인이 셋 겹쳤고, **셋 다 "검출기가 침묵한 것"을 "카피가 깨끗한 것"으로 보고해 온** 같은 실패다.

- **따옴표 마스킹이 카피를 통째로 지우고 있었다** — `mask()`가 큰따옴표 안을 "인용 보호"로 공백 치환하는데, i18n·TS·JSON 카피는 **100%가 큰따옴표 안**이다. 검사 대상이 빈 파일이었으므로 이 파일들은 어떤 규칙에도 걸릴 수 없었다. `--genre UI`에서는 따옴표 안이 곧 본문이므로 한글 인용부(`“ ”`)만 보호한다.
- **§8 내부 용어 노출 신설** — 기존 7개 카테고리는 전부 *문체*(번역투·상투구·피동·리듬)라, **문체는 멀쩡한데 어휘가 기능 명세서에서 그대로 온** 경우를 잡을 규칙이 아예 없었다. 판정 기준은 하나 — 읽는 사람이 쓰는 말인가, 만든 사람이 쓰는 말인가. `스펙`·`법률`·`공적`은 전면 면제(거기선 "읽기 전용"이 정확한 용어다).
- **KOH17 신설 — SSOT에만 있고 검출기엔 없던 규칙** — rules.md §1이 `"~할 수 있다" 습관성 남발`을 🟡로 규정해 놓고 대응 정규식이 없었다. 그 랜딩 ko 블록에 12회 있었다. 헌법 53회 실측 근거로 법률·공적·스펙 면제 + 밀도 게이트.
- **한글 종성은 낱자 클래스로 못 잡는다** — 첫 구현의 `[을를ㄹ] 수 있`이 "**할** 수 있"을 0회로 셌다. '할'은 U+D560 한 글자라 종성 ㄹ이 따로 없다. `[가-힣] 수 있`으로 교정. 정규식이 조용히 0을 반환하는 것이 이 스킬에서 반복된 실패 모드다.
- **UI 장르는 분모가 다르다** — 개행을 문장으로 세면 i18n 파일은 줄 대부분이 코드·영문이라 분모가 부풀어 밀도 규칙이 영영 발화하지 않는다. 서술어로 끝나는 줄만 발화 단위로 센다(라벨을 분모에 넣으면 다시 부푼다).
- **산문 규칙은 UI에서 끈다** — 연결어미 쉼표·문두 접속사·"것이다" 연발·콜론 제목·예고 공식. 버튼 라벨과 eyebrow에 산문 리듬 규칙을 들이대면 UI가 깨진다. 종결어미 4연속·문장 길이 다양화도 적용하지 않는다(필드마다 독립 문자열이라 리듬을 논할 단위가 아니다).
- **`랜딩`과 `UI`를 갈랐다 — gpt-5.6-sol 교차검증 결과** — 초안은 "읽기 전용/편집 화면/작업 화면/지원 범위/적용 위치"를 §8 🔴 한 덩어리로 묶었다. sol이 "일반 사용자도 자연스럽게 쓰는 말"이라며 오탐 위험을 지적했고 그게 옳았다: **파일 권한 드롭다운의 "읽기 전용"은 정확한 용어다.** 위험한 건 어휘가 아니라 **쓰인 자리**였다. 어휘 목록을 손보는 대신 장르 축을 하나 더 세워, 같은 단어가 `랜딩`(설득하는 문장)에서는 🔴, `UI`(조작하는 화면)에서는 🟡이 되게 했다. KOH18(어디서도 변호 불가)과 KOH20(문맥 의존)으로 분리.
- **KOH19 확장** — 실측 코퍼스(nolpop 한글 문자열 329줄)에서 "정상적으로", "해당 OO", "처리했습니다"를 확인해 🟡로 추가. 반대로 **"처리"와 "사용자"는 넣지 않았다** — 실측하니 "개인정보처리방침"·"처리업체"(법정 용어)·"사용자 1명"(좌석 수)처럼 정당한 쓰임이 대부분이었다. 후보를 넣기 전에 세는 것이 이 룰북의 캘리브레이션 방식이다.
- **forge·page-craft에 배선** — 검출기를 고쳐도 부르는 데가 없으면 소용없다. `sg-landing-forge` P6 배터리(`audit.mjs` 게이트 1b)와 `sg-page-craft` P3 검사에서 실제 렌더 카피를 받아 돈다. 자세한 내용은 두 스킬의 1.6.4 항목.
- **1.6.3의 "메시지 파일 자동 수집 제외"는 유지한다** — 다만 이유를 정정했다. ⓑ("룰북이 산문용")는 `UI` 장르가 해소했고, 남은 진짜 사유는 ⓐ 초단문의 불변식 커버리지 0 · ⓒ en/ko 병렬 쌍 정합 · **용어 표류**("요금제"와 "플랜"을 한 파일에서 혼용)다. 셋 다 여전히 기계 관할 밖이고, 그렇게 적어 둔다.
- 회귀: 산문 문서(SKILL.md·rules.md) 오탐 0 · `스펙` 장르에서 §8 침묵 확인 · 문제의 랜딩 파일에서 🔴1(KOH18 ×4) 🟡1(KOH17 ×12) 검출로 exit 1.

## 1.6.3 — 2026-08-19 (내용 불변 게이트 신설 — "부정 반전이 통과하던" 사고)

감사 실측: **"환불이 불가능합니다"→"가능합니다", "책임지지 않습니다"→"책임집니다"가 4.1% "OK 수렴"으로 통과**했다. 유일한 게이트인 `change_rate.py`가 `SequenceMatcher` 비율 하나만 재서 **무엇이 바뀌었는지는 전혀 몰랐다.** 폴더 전체 무인 완주가 기본이고 git 폴더면 백업도 안 만드는 스킬에서, 정책·약관 파일이 사정권에 있었다.

- **불변식 축 신설** — 숫자(단위 1어절 결합: 7일→7주·3만→3억 차단)·조항(제N조)·부정·극성·조건 표지·URL·영문 고유명사를 대조. **불변식 위반은 변경률과 무관하게 즉시 중단**이 하드 라인이다.
- **문장·절 쌍 단위 비교** — 파일 전체 총량만 세면 재배치·상쇄가 통과한다(실측: 작용 대상 교환, 숫자 자리 교환, 한쪽 부정 삭제를 다른 쪽 "아니라"로 메꾸기). 짝을 지어 짝 안에서만 대조한다.
- **유사도 기반 재짝짓기** — 정확 일치 정렬은 양쪽으로 터졌다: 문장 하나를 추가해 개수를 어긋내면 공격이 부활하고(통짜 블록 비교), 순수 재배열은 오탐이 났다. **네 결함의 뿌리가 하나**였고 `ratio ≥ 0.6` 그리디 재짝으로 넷이 함께 닫혔다. 헌법 실측: 제자리 윤문 191건 → 오탐 0, 거기에 줄 교환 2곳 추가 → 이전 ABORT(위반 4)에서 **통과**로.
- **부정 판정 정교화** — `(?<!잘)못`(잘못하면 오탐), `안(?=\s?(되|됩|돼|된))`(맞춤법 교정 오탐 + "안 됩니다" 미검출 버그), `아니(?!라)`(룰북이 평서문화를 지시하는 수사 구문), 굳어진 부사(관계없이·틀림없이) 제외, `마십시오|말 것` 추가(법적 경고문의 표준 금지형이 빠져 있었다).
- **극성은 "소실"이 아니라 "짝 교체"** — 소실만으로 잡으면 "면 **선택**"의 '선택'을 필수↔선택으로 오인한다(실측). X가 줄고 반대말 Y가 늘 때만 위반. `"-지 않"` 프레임쌍(적지 않↔많지 않) 포함.
- **짧은 텍스트는 변경률 대신 2-gram 유지율, 그것도 WARN까지** — 하드 라인은 내용이고 분량은 사람이 본다. 한 줄 카피의 정당한 교정이 막히던 문제 해소.
- **`scripts/detect_ko.py` 신설** — 🔴 패턴 16종 정규식 판정, 장르 면제(법률·스펙), 밀도 임계, 코드펜스·URL·front-matter 마스킹, stdin 지원. forge-rules §5 "🔴 패턴 0"의 판정자가 **양쪽 어디에도 없던** 공백을 닫는다.
- **연결어미 뒤 쉼표 🔴 → 🟡 강등 + 법률 전면 면제** — 1987년 헌법 원문에서 `-고,` 15회·`-며,` 49회 실측. "AI 한국어의 대표 지문"으로 규정하면 헌법이 64곳 위반이 된다.
- **막지 못하는 것을 문서에 명시** — 맨몸 반의어 반전·한글 고유명사 단독 실행·이중부정 해소 오탐. 막는 척하지 않는다.
- **`--mode=code` 신설** — i18n 메시지 파일용. 1급 불변식은 **skeleton 동등성**(문자열 리터럴 본문만 치환한 소스가 byte-동일)이라 키·보간·삼항·주석이 자동 동결되고, 값은 위치로 1:1 대응돼 값 쌍마다 문장 쌍 불변식이 그대로 돈다. 미지원 구문은 판별하지 않고 **"관할 밖 — 수정 금지" exit 2**(지원 못 하는 파일을 조용히 통과시키는 것이 최악의 실패 모드다). 실측: 실제 메시지 파일 6개 전수 무결성 통과·폴백 0건.
- **그런데 메시지 파일을 배치 자동 대상에서 뺐다.** 게이트를 만든 뒤 내린 결론이다 — 게이트가 결정적으로 막는 건 **코드 파손**뿐이고, ⓐ 초단문 UI 카피는 불변식 커버리지가 0으로 수렴하며("저장"→"삭제"도 통과) ⓑ 이 룰북은 산문용이라 버튼 라벨엔 부적합하고 ⓒ en/ko 병렬 쌍 정합은 문자열 단위로는 못 본다. **계측기가 생겼다고 문을 여는 게 아니다** — 자동 수집에서 빼고 명시 지정 경로의 안전장치로만 쓴다. description의 "사이트 전체" 공약도 정직하게 축소.
- 검증: 공격 17종 중 16 차단(유일 통과는 문서화된 미방어 항목) · 정당 편집 13종 중 12 통과 · 헌법 40KB 실문서 3종(191편집·재배열·회귀) 전부 통과.

## 0.1.0 — 2026-08-14

- 최초 생성

## 0.1.1 — 2026-08-15

- description에서 AI 모델명 나열(ChatGPT·Claude·Gemini) 제거 — 윤문 대상 글의 출처 예시였으나 지원 플랫폼 나열처럼 읽혀 혼동 유발.

- description 압축 — 트리거·비대상 보존, 절차 설명 제거 (상시 로드 비용 절감)
