# sg-landing-craft — 검수 기준 (SSOT)

> 제작물의 하한선과 완성선. 수치 판정은 `scripts/detect.mjs`(룰 ID는 `scripts/rules-lib.mjs`와 1:1)·`scripts/probe.mjs`가 내고, 구성 판정은 캡처를 실제로 본다.

## 1. 하한선 — detect.mjs가 집행 (🔴 0 · QF 0이 통과 조건)

37룰 자동 탐지. 제작 중 특히 밟기 쉬운 것만 요약(전체는 detect 출력이 SSOT):

- **AI 티**: 보라 그라데이션(CO1)·그라데이션 텍스트(CO5)·빈 카드 3장 나열(LA1)·필 뱃지-위-제목(CP1)·번호 뱃지 카드(CP2)·이모지 아이콘(IC2)·화살표 글리프 텍스트(IC5)·사이드 액센트 보더(BD1)·장식 블롭(IC4)
- **자산**: 동일 이미지 3회+ 재사용(AS1 — 이 스킬에선 철칙 위반이기도 하다)·플레이스홀더(AS2)
- **밀도**: 여백 지배 섹션(DE1)·저밀도 카드 행(DE2) — 처방은 압축→병합→"콘텐츠 부족" 보고, 장식으로 채우기 금지
- **품질 바닥(QF)**: 가로 오버플로·정지 시 숨은 텍스트(리빌은 기본 가시로 설계)·행폭 과대(한글 42자)·타이포 하한·모바일 44px 탭 타깃·unsized img
- **한글 조판(KO)**: 행간<1.5·이탤릭·양수 자간·한글 폰트 미지정 + (스크립트 밖) **keep-all 전역**, 2분할 강조가 어순을 깨는지

## 2. 인터랙션 언어 — probe.mjs가 집행 (라이브러리 교차 실측이 근거)

- 호버: **색 전환·옅은 알파 필·brightness만** — transform·그림자 금지 (toss·linear 실측)
- duration **1~2개 값으로 수렴**(0.1~0.2s), easing 1개, `transition: all` 0개
- 무반응 인터랙티브 요소 0 목표(로고는 관례상 예외), 리빌은 1회성·재은닉 없음
- active 프레스 피드백·focus-visible·disabled 상태 정의 (마감 체크)

## 3. Squint 5축 — 캡처를 실제로 보고 판정 (제작 중 + 완료 후 2회)

① 초점 부재(전부 동등 경쟁) ② 평평한 위계(무게+명도 사다리로) ③ 단조 레이아웃(구역별 밀도 차등) ④ 소심한 색(액센트 1색 ~10%) ⑤ 보더로 만든 구조(간격·서피스 단차로)

## 4. 퀄리티 바 — 완성선 (보고서에 항별 근거)

1. **디스플레이 모먼트** — 히어로 헤드라인 ≥ 본문 4배(라이브러리 실측 4.1~7.2×)
2. **한 화면 동시 위계 ≤ 6단** (총 단 수보다 이게 실제 규칙 — toss 실측)
3. **자산 플랜 이행** — plan.md의 파일 목록과 실제 배치 일치, 재사용 0
4. **색 커밋** — 지배 1 + 강조 1이 전 섹션 관통
5. **빈 공간은 의도일 때만** — 실증(실화면·수치·후기)으로 채우거나 섹션을 접는다
6. **차용 이행** — borrow.md의 각 항목이 실제 구현에 반영됐는지 대조(안 쓴 차용은 삭제하고 사유 기록)

## 5. 콘텐츠 검수 (이 스킬 고유)

- 모든 주장·수치가 `facts.md`의 출처와 1:1 대응 — 대응 없는 문장은 삭제
- 법적·정직성 고지(예: "시안은 실제 색과 다를 수 있음") 누락 여부 — facts의 "제약·고지" 항목 전수 반영
- 카피에 sg-ko-humanize 🔴 패턴(상투구·과장·번역투) 0
- 금지: 제품이 안 하는 것(로드맵·미출시 기능)을 하는 것처럼 쓰기

## 6. 같은 형태 반복 금지

`_landing/history.json`의 마지막 실행과 **베이스 팔레트 계열 상이**(rules-lib `samePaletteFamily` — detect 산출물 `page.bodyBg`로 대조) + **7축 중 3축 이상 상이**(directions.md). 완료 시 이번 실행 append.

## 등급

- **A**: 🔴 0 · QF 0 · 퀄리티 바 6/6 · 콘텐츠 검수 통과 · probe 언어 수렴
- **B**: 🔴 0 · 퀄리티 바 5/6
- **C**: 🔴 1+ 또는 퀄리티 바 4/6 이하 — 재작업
- **D**: 사실 무근 카피 발견 — 채택 금지(콘텐츠 신뢰가 전부다)
