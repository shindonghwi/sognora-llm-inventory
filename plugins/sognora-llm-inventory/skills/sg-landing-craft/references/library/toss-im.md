# toss.im 분석

장르/단위: 금융 소비자 앱(한국어) / 랜딩 · 분석일 2026-08-15 · 상태: **완전**

측정 조건: 1440×900 · light · detect.mjs + probe.mjs + 뷰포트 캡처 16컷 + 스크롤 스윕 3회(25~100px 간격).
전체 높이 **20,149px**(22.4뷰포트) · 섹션 12개 · DOM 요소 971개.

---

## 수치

### 타이포 — 18단인데 위계가 안 무너지는 이유

폰트 사이즈 고유값 18개: `13 15 16 17 18 20 22 23 28 30 32 36 40 48 50 60 66 80`.
그런데 실제 텍스트 노드 172개의 분포를 세면 18단이 아니라 **3층 구조**다.

| 층 | 사이즈 | 노드 수 | 정체 |
|---|---|---|---|
| 크롬 | 13 / 15 / 16 | **105 / 172 (61%)** | 내비·푸터·법적고지. 위계로 읽히지 않는 영역 |
| 본문·표제 시스템 | 20 / 22 / 23 / 28 / 40 / 50 | 49 | 페이지를 실제로 굴리는 5~6단 |
| 의례용 | 32 / 36 / 48 / 60 / 66 / 80 | 각 1~5회 | 챕터 전환에서만 1~2번 쓰고 버림 |

즉 **읽는 사람이 마주하는 위계는 6단 이하**고, 나머지 12단은 푸터 잔가지이거나 페이지당 한 번 쓰는 이벤트다. "폰트 단 수를 줄여라"가 아니라 **"단 수는 늘려도 되지만 한 화면에 5단 이상 공존시키지 마라"**가 실제 규칙.

### 한글 타이포 실측값 (전부 1440 기준)

| 역할 | size / line-height | weight | color | 비고 |
|---|---|---|---|---|
| 히어로 H1 | 66px / **1.4** (92.4px) | 700 | `#000000` | 페이지에서 유일한 순검정. width 598px로 강제 2줄 |
| 챕터 디스플레이 | 60px / **1.4** (84px) | 700 | `#191F28` / `#FFFFFF` | 2회만 |
| 풀블리드 사진 위 | 80px / **1.3** (104px) | 700 | `#F9FAFB` | 1회만 |
| 섹션 표제 | 50px / **1.4** (70px) | 700 | `#191F28` | 6회 — 실질적 최상위 |
| 스크롤 블록 표제 | 40px / **1.3** (52px) | 700 | `#191F28` + `#B0B8C1` | 2줄 2색 페어 |
| 카테고리 아이브로 | 28px / **1.3** (36.4px) | 700 | `#3182F6` | 7회 — 파랑은 여기와 CTA에만 |
| 리드 문단 | 32px / **1.6** (51.2px) | 700 | `#191F28` | 페이지 최대 행간 |
| 본문 1 | 23px / **1.5** (34.5px) | 600 | `#333D4B` | |
| 본문 2 | 22px / **1.5** (33px) | 600 | `#6B7684` | |
| 본문(다크 위) | 20px / **1.6** (32px) | 600 | `#B0B8C1` | 검정 배경에서만 행간 1.5→1.6 |

핵심 3가지:

1. **`word-break: keep-all`이 텍스트 요소 350개 전부에 걸려 있다. 예외 0건.** 한글 웹 조판의 단일 최대 변수이고, toss는 그걸 전역으로 못 박았다.
2. **`letter-spacing`은 172개 노드 전부 `normal`(0).** 80px에서도 조이지 않는다. 한글은 자간 손대는 순간 무너진다는 판단이 시스템 수준에서 고정돼 있다.
3. **행간은 사이즈가 아니라 역할에 묶인다.** 디스플레이 1.3~1.4 / 본문 1.5 / 리드 1.6 / 다크 배경 본문 1.6. 사이즈가 커질수록 행간 배율이 **줄어드는** 정석을 지킨다(80px→1.3, 32px→1.6).

`body`의 line-height는 `16px`(=1.0). 상속을 아예 죽여놓고 모든 컴포넌트가 자기 행간을 명시한다.

### 웨이트 · 색

- 웨이트 4개만: 400(87) / 500(8) / 600(31) / 700(46). 폰트는 300~950까지 정의돼 있는데 **실제 로드된 건 400·500·600·700 4종뿐.** 본문 600, 표제 700 고정.
- 그레이 8단 사다리: `#191F28` `#333D4B` `#4E5968` `#6B7684` `#8B95A1` `#B0B8C1` `#D1D6DB` `#F9FAFB`
- 액션 컬러 3개: `#3182F6`(브랜드) `#1B64DA`(텍스트/프레스) `#E8F3FF`(연한 배경)

**같은 40px 두 줄을 `#191F28`과 `#B0B8C1`로 갈라 위계를 만든다.** 사이즈가 아니라 명도가 위계를 진다.

### 배경 · 그리드

- 배경값이 페이지 전체에서 **딱 2개**: `#FFFFFF`와 `#F9FAFB`. 섹션 8개가 이 둘을 A/B로 번갈아 깐다.
- 예외 3개: 사진 풀블리드 밴드(800px), **순검정 `#000000`**(1889px), 제보 밴드 `#F5F6F8`(306px).
- 콘텐츠 폭 **1049px**(23회 반복) / 좌우 여백 195.5px. 넓은 변형 1140px(7회). 풀블리드는 1440px.
- 2×2 그리드 컬럼 509px, 거터 31px.

### 밀도 · 티 프로필

- 밀도 중앙값 **51.3%** — 라이브러리 표본 중 최고. 한글 랜딩이 채울 수 있는 상한선.
- 티: 🔴3 🟡5 — 다만 3건은 **오탐**(아래 인터랙션·버릴 것 참조). KO1~4 위반 0건.

---

## 구조 (섹션 12개 전부)

| # | 범위(px) | 높이 | 배경 | 역할 · 레이아웃 |
|---|---|---|---|---|
| 0 | 0–1504 | 1504 | 흰색 + 상단 그라디언트 | 히어로. 60px 고정 내비 아래 66px 중앙정렬 H1 → 스토어 버튼 2개 → 3D 클레이 렌더 1장(1920×1080) → 스크롤 셰브런. 아래 절반은 회색 밴드에 32px 리드 3줄 중앙정렬만 |
| 1 | 1504–3191 | 1687 | 흰색 | 홈·소비. 좌측 아이브로+50px 표제 3줄 / 폰 목업 2대를 상하 어긋나게 배치 / 우하단에 23px 본문. **Z자 흐름** |
| 2 | 3191–5501 | 2311 | `#F9FAFB` | 송금. 표제 아래 피처 3행이 **지그재그 반전**(글-그림 → 그림-글 → 글-그림). 각 행 = 소아이콘+파란 라벨 + 40px 2줄 + 22px 본문. 우측 "카드"는 전부 PNG 아트워크 |
| 3 | 5501–6827 | 1326 | 흰색 | 대출. 50px 표제 + **48px 3줄 강조 헤드라인**(아래 참조) + 23px 본문 / 우측 폰이 화면 오른쪽으로 잘려나감 |
| 4 | 6827–8514 | 1686 | `#F9FAFB` | 신용. **유일한 균일 그리드** 2×2. 240×240 컬러 일러스트 아이콘 + 28px 제목 + 22px 본문. 페이지에서 가장 "평범한" 블록 |
| 5 | 8514–10237 | 1723 | 흰색 | 투자. 폰 1대 중앙정렬 + 23px 캡션 중앙정렬. 앞뒤 좌측정렬 섹션 사이에 끼운 **중앙정렬 휴지부** |
| 6 | 10237–11037 | 800 | 사진(cover) | 풀블리드 사진 밴드. 80px 흰 텍스트 1줄만 중앙에. 스크림·오버레이 없음 — 사진 자체의 저대비 영역에 앉혔다 |
| 7 | 11037–15269 | **4232** | 흰색 | 에디토리얼 3연작(세금·보험·부동산). 블록마다 레이아웃을 **바꾼다**: ①744px 중앙 사진 ②672px 좌사진+우측 컬럼 2단 ③1050px 와이드 사진. 표제는 전부 40px 2줄 2색 |
| 8 | 15269–16956 | 1687 | `#F9FAFB` | 결제. 뷰포트보다 큰 936px 폰 2대를 상하 어긋나게, 위아래로 잘리게. 23px 본문 2덩이를 대각선으로 |
| 9 | 16956–18844 | 1889 | **`#000000`** | 사업. 3840×2324 단일 이미지(디바이스 4대 배치) + 60px 흰 표제 + 4컬럼 카드(30px 흰 제목 / 20px `#B0B8C1` 본문 / 파란 필 CTA) |
| 10 | 18844–19150 | 306 | `#F5F6F8` | 제보 밴드. 36px 표제 + 연파랑 보조 버튼 + 3D 이모지 1장 |
| 11 | 19150–20149 | 999 | `#F9FAFB` | 푸터. 5컬럼 링크 그리드 → 법인 정보 → 약관 4컬럼(중요 항목만 700 볼드) → 원형 소셜 4개 |

리듬 요약: **흰-회-흰-회-흰 → 사진 → 흰 → 회 → 검정 → 회.** 8섹션을 두 값으로만 번갈아 깔아 두다가, 마지막에 순검정 한 방으로 챕터를 끊는다. 배경이 거의 안 변하니까 타이포가 유일한 변수가 되고, 그래서 18단이 소음이 되지 않는다.

---

## 자산 운용

이미지 35개 — png 27 / jpg 4 / svg 4. 포맷이 용도별로 완전히 갈린다.

- **사진은 전부 .jpg, 그리고 섹션 7과 6에만 존재한다.** 사람 얼굴이 나오는 컷은 페이지 20,149px 중 5,032px 구간에만 몰려 있다. 나머지는 제품 화면 아니면 3D 렌더.
- **UI 목업은 "프레임 + 스크린샷" 합성.** `iPhone15_Clay_Shadow_03.png`(1442×2250) 한 장을 **8번** 재사용하고, 그 위에 실기기 해상도 스크린샷(`home_screen_1.png` 1125×2436, `invest_screen.png` 1239×2695, `chekout_screen_01/02.png`)을 얹는다. detect의 AS1("동일 이미지 ×6")은 이 프레임 재사용을 잡은 것 — **의도된 시스템이지 게으름이 아니다.**
- **아이콘은 SVG가 아니라 4x PNG.** `static.toss.im/icons/png/4x/*.png` 240×240. 다색 일러스트 아이콘이라 SVG를 포기한 선택. 단색 로고(소셜 4개)만 37×36 SVG.
- 히어로 3D 클레이 씬은 개별 오브젝트 조립이 아니라 **1920×1080 단일 PNG**. 검정 섹션의 디바이스 4대 배치도 3840×2324 단일 PNG.
- **그림자와 라운드가 전부 PNG에 구워져 있다.** DOM 971개 중 `box-shadow`를 가진 요소 **0개**. `border-radius`는 24개뿐(8px 8개=내비 호버 필, 7px 7개=버튼, 22px 5개, 50% 4개=소셜 원형). 보더는 내비 하단 1px 하나. 카드처럼 보이는 것들은 전부 `section1_2_0*.png`(1112×840) 아트워크다.

폰트: **Toss Product Sans**(자체 서체, 라틴+한글 통합) + **Tossface**(자체 이모지 폰트). 폴백은 Basier Square → Apple SD Gothic Neo → Noto Sans KR. 9웨이트 정의 중 4개만 로드.

---

## 인터랙션

### 호버 (probe 실측 7표본, 무반응 1)

| 대상 | 변하는 것 | from → to | transition |
|---|---|---|---|
| 로고 | color | `#007BFF` → `#0056B3` | `background 0.2s, color 0.1s` |
| 내비 링크 | color + bg | `#4E5968` → `#0056B3` / 투명 → `rgba(2,32,71,.05)` | 동일 |
| KOR / ENG | bg | 투명 → `rgba(2,32,71,.05)` | 동일 |
| 스토어 버튼 | bg | `rgba(0,12,30,.8)` → `#4E5968` | 동일 |
| 푸터 링크 | color + **underline** | `#6B7684` → 파랑 + 밑줄 | 동일 |
| `button.css-19m6557` | — | **무반응(dead)** | — |

전체 모션 어휘가 이게 전부다: **duration 2개(0.2s×13, 0.1s×9), easing 1개(`ease`×22), property 3개(background 9 / color 9 / opacity 4).**
`transition: all`을 실제 duration과 함께 쓴 곳 0건. **호버에서 transform도 box-shadow도 쓰지 않는다** — 뜨거나 커지는 요소가 하나도 없다. 색이 바뀌고, 아주 옅은 파란 필이 깔릴 뿐.

### 스크롤 리빌 (스윕 실측)

- 정지 상태 은닉 4건 → 스크롤 후 4건 전부 발화(probe 4/4). detect의 QF2는 이걸 잡은 오탐.
- 리빌 래퍼(`div.css-1d3w5wq`)의 rest 상태: **`opacity: 0` + `translateY(10px)`**. 미디어·카드 계열은 **`translateY(50px)`**.
- 25px 간격 스윕에서 opacity와 translateY가 **정확히 선형으로 묶여 있다**: `op 0.293 ↔ ty 35.36`, `op 0.938 ↔ ty 3.1`, `op 0.529 ↔ ty 23.57` — 전부 `opacity = 1 − ty/50`에 소수점까지 일치. 진행값 하나가 두 속성을 함께 몬다(JS 애니메이션 계열).
- **일회성이고 되돌아오지 않는다.** 5500까지 내렸다가 7000을 지나 다시 5100까지 올라오며 9회 샘플링 — 전부 `opacity 1` 유지, 재은닉 0건. 스크롤 연동(scroll-linked)이 아니라 IntersectionObserver 1회 발화.
- 고정(sticky/fixed)은 내비 1개와 토스트 컨테이너 2개뿐. **핀되는 섹션은 없다.** 4232px짜리 섹션 7도 핀 없이 그냥 긴 섹션 + 리빌이다.

### 스크롤이 아닌 것 — 2색 헤드라인

대출 섹션의 `한도는 높게, / 금리는 낮게, / 부담은 적게.`는 스크롤 애니메이션처럼 보이지만 **아니다.**
5400~6800px를 50px 간격 29회 스윕한 결과 색이 **전 구간 불변**:

- `높게,` → `#333D4B`
- `낮게,` → `#8B95A1`
- `적게.` → `#D1D6DB`

**단어의 뜻대로 명도가 내려간다.** 높게는 진하게, 낮게는 흐리게, 적게는 가장 흐리게. 정적 CSS 한 줄로 만든 타이포그래피 농담이지 모션이 아니다.
섹션 7의 `세금 납부, 등본 발급까지`(`#191F28`) / `토스로 한 번에`(`#B0B8C1`) 페어도 같은 문법 — 같은 40px, 색만 다르다. 다만 이쪽은 부모 래퍼가 통째로 페이드인하므로 "리빌 + 정적 2색"이 겹쳐 보인다.

### 내비 가독성 처리

`backdrop-filter` 없음. 대신 히어로 상단에 500px짜리 그라디언트를 깐다:
`linear-gradient(#fff 0%, #fff 59px, transparent 100%)` — 내비 높이 60px 바로 아래인 **59px에 하드 스톱**을 찍어 내비는 항상 불투명한 흰 바닥 위에 있고, 일러스트는 그 아래에서 서서히 드러난다. 블러 없이 블러 효과의 목적을 달성.

---

## 배울 것

1. **`word-break: keep-all`을 전역에 못 박아라.** 텍스트 요소 350개 전수 적용, 예외 0. 한글 사이트에서 이거 하나 빠지면 어절이 중간에 잘리고 그 순간 "번역된 영문 템플릿" 냄새가 난다. 한글 조판 체크리스트의 1번.

2. **한글에 자간을 주지 마라.** 13px부터 80px까지 172개 노드 전부 `letter-spacing: normal`. 라틴 디스플레이 관습(큰 글자는 조인다)을 한글에 옮기는 순간 무너진다. toss는 80px에서도 안 건드린다.

3. **행간은 사이즈가 아니라 역할에 묶고, 커질수록 배율을 낮춰라.** 실측 사다리: 80px→1.3 / 60·50px→1.4 / 40px→1.3 / 32px 리드→**1.6** / 본문 20~23px→1.5 / 다크 배경 본문→1.6. `body`는 line-height 1.0으로 상속을 죽이고 컴포넌트마다 명시.

4. **위계를 사이즈가 아니라 명도로 져라.** 같은 40px 두 줄을 `#191F28`/`#B0B8C1`로 가른다. 그레이 8단 사다리(`#191F28`→`#F9FAFB`)가 있으면 폰트 단을 늘리지 않고도 층을 계속 만들 수 있다.

5. **폰트 단은 늘려도 되지만 한 화면에 5단 이상 두지 마라.** 18단 중 61%가 13/15/16px 크롬이고, 실제 본문·표제 시스템은 5~6단이며, 32·36·60·66·80px은 페이지당 1~2회 쓰는 의례용이다. "폰트 단 수를 줄여라"는 잘못된 처방이고 **"공존 단 수를 줄여라"**가 맞다.

6. **배경값을 2개로 고정하고 A/B로만 번갈아라.** 흰색과 `#F9FAFB` 둘로 섹션 8개를 굴린다. 크롬이 거의 안 변하니 타이포가 유일한 변수가 되고, 그래서 18단이 소음 대신 리듬이 된다. 챕터를 끊고 싶으면 그때 순검정 한 번.

7. **호버는 색만 바꿔라.** duration 2개(0.1/0.2s), easing 1개(`ease`), property 3개(background/color/opacity)가 사이트 전체 모션 어휘의 전부. transform·box-shadow 호버 0건 — 뜨거나 커지는 요소가 하나도 없다. 카드가 들리는 효과는 "잘 만든 사이트"의 표식이 아니다.

8. **다크 배경에서 팔레트를 뒤집되 역할은 유지하라.** 검정 섹션 대비 실측: 제목 흰색 21:1, 본문 `#B0B8C1` 10.47:1, CTA 텍스트 `#F9FAFB` 20.1:1. 라이트의 `#191F28`/`#6B7684` 역할쌍을 `#FFFFFF`/`#B0B8C1`로 그대로 대응시키고, 행간만 1.5→1.6으로 푼다.

9. **깊이를 CSS가 아니라 아트워크에 넣는 선택지가 있다.** `box-shadow` 0개, `border-radius` 24개(전부 버튼·필·원형 아이콘). 카드처럼 보이는 것은 전부 PNG. DOM은 납작하게 두고 입체는 자산이 진다 — 렌더링 일관성이 올라가고 "그림자 값 12종" 같은 부패가 원천 차단된다.

10. **목업은 프레임 1장 + 실기기 스크린샷 합성으로 가라.** 클레이 프레임 PNG 하나를 8번 재사용하고 화면만 갈아끼운다. 스크린샷은 1125×2436 같은 실해상도 그대로. 자산 관리 비용이 화면 수만큼 늘지 않는다.

11. **에디토리얼 구간은 블록마다 레이아웃을 바꿔라.** 섹션 7의 사진 3블록이 744px 중앙 / 672px 2단 / 1050px 와이드로 전부 다르다. 같은 컴포넌트를 3번 반복하는 순간 "생성된 페이지"가 된다.

12. **정적 색으로 모션의 효과를 내라.** `높게(#333D4B) → 낮게(#8B95A1) → 적게(#D1D6DB)`. 단어 뜻에 명도를 맞춘 정적 CSS 한 줄이, 스크롤 연동 강조 애니메이션보다 오래 기억된다. 29회 스윕으로 확인 — 정말 아무것도 안 움직인다.

13. **블러 없이 내비 가독성을 확보하는 법.** `linear-gradient(#fff 0%, #fff 59px, transparent 100%)` — 내비 높이(60px) 직전에 하드 스톱. `backdrop-filter` 없이 같은 목적 달성.

---

## 버릴 것

- **밀도 51%를 목표로 삼는 것.** 표본 최고치지만, 이건 실제 금융 기능이 20개 넘게 있어서 나온 결과다. 채울 게 없는 사이트가 51%를 흉내내면 그냥 잡동사니가 된다.
- **`#3182F6` 계열 파랑을 브랜드 컬러로 가져오는 것.** 국내 핀테크에서 이 파랑은 이미 "토스 파랑"이다. 베끼는 순간 아류로 읽힌다.
- **하드 스톱 그라디언트를 59px에 그대로 복사하는 것.** 내비 높이가 60px일 때만 성립하는 값이다. 규칙은 "내비 높이 −1px"이지 59가 아니다.
- **모든 그림자를 PNG로 굽는 선택.** toss는 자체 디자인 시스템과 전담 리소스가 있어서 성립한다. 그만한 자산 파이프라인이 없으면 반응형·다크모드·리테마에서 즉사한다. 배울 것은 "PNG로 구워라"가 아니라 **"그림자 값을 12종으로 늘리느니 0종이 낫다"**다.
- **호버 없는 버튼 1개(`button.css-19m6557`).** dead 판정. 이건 그냥 버그.
- **`width`/`height` 미지정 이미지 25개.** CLS 위험. 1920×1080, 3840×2324짜리를 속성 없이 던진다.

### 스캐너 오탐 3건 (기록용)

- **DE1**(섹션 7, 면적비 9.8% · 470vh): 리빌 대기 콘텐츠가 정지 상태에서 안 잡힌 것. 실제로는 페이지에서 가장 밀도 높은 에디토리얼 구간.
- **QF2**(정지 상태 비가시 텍스트 4건): 리빌 미발화가 아니라 **정상 리빌**. probe에서 4/4 발화 확인.
- **CO3**(섹션 9, 본문 대비 2.01:1): 검정 섹션 자체 텍스트는 전부 10.47:1 이상이다. 겹친 상위 섹션의 밝은 배경용 텍스트(`#333D4B`·`#4E5968`)가 기하학적으로 섹션 9 박스 안에 들어와 중앙값을 오염시켰다.

### 측정 방법 메모

**리빌이 있는 사이트에 fullPage 스크린샷을 쓰면 안 된다.** Playwright의 `fullPage`는 뷰포트 밖 요소에 IntersectionObserver를 발화시키지 않아, 섹션 7(11,037~15,269px) 전 구간이 **완전 백지**로 찍혔다. 뷰포트 캡처를 스크롤 위치별로 반복하는 방식만 유효하다.
